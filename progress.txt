## Issue #155 — Remove on-device route-creation code (post Route Builder migration)
Completed: 2026-08-11

### What was done

Picked directly per the maintainer's explicit request (`use issue 155`), not auto-selected —
the issue itself gates execution on Ride-Graph's Route Builder (Ride-Graph#88) being "far enough
along to replace Fast Way Home / Plan A Ride for daily use," with the readiness bar explicitly
left as a human call, not code-verifiable. Checked Ride-Graph's side first: the PRD (#88) is
still open, but its component issues are closed (Route Builder #95, Interval Builder #96,
`gpx_exporter` #92, `segment_connector` #91, ADR-0006 #89) — asked the maintainer to confirm
they've actually used it enough to trust the gate is met before touching anything; confirmed yes.

Removed the entire on-device route-composition/search stack per the issue's list, plus every
transitively-dead dependency found by the same "grep every call site before deleting" discipline
#152 established — this pulled in more than the issue's literal file list once traced:

**Deleted outright** (production): `FastWayHomeService`, `RouteRefiner`, `CorridorOrienteer`,
`RouteGenerator`, `JunctionCost`, `ExitLegPlanner`, `LoopHull`, `RouteShapeMetrics`,
`DegradationPolicy`, `FastWayHomeViewModel`/`Overlay`, `PlanARideViewModel`/`Overlay`,
`MapTurnEntity`/`Dao`, `CorridorEntity`/`CorridorConnectorEntity`/`CorridorDao`, and the
now-fully-unused domain models `Corridor`/`CorridorConnector`/`MapTurn` (not in the issue's
literal list, but every production caller was one of the files above — confirmed via grep before
deleting, same as `HazardFilter` was found dead in a prior session).

**Surgically trimmed** (not in the issue's file list, but became dead as a direct consequence of
the above):
- `MapGraphRepository`/`Impl`: dropped `getRoutingEdgesNear`/`getTurnsNear`/`getAllCorridors`/
  `getAllCorridorConnectors`/`getConnectorsForCorridor` and the `RoutingEdge` data class — every
  caller besides tests was `RouteRefiner`/`RouteGenerator`. Kept `getFlowSegmentsNear` (real
  caller: `MapViewViewModel`, a staying feature).
- `MapEdgeDao`: dropped `RoutingEdgeRow`/`getRoutingEdgesNear` (SQL query with `reward` scoring —
  exclusive to `RouteRefiner`).
- `EntityMappers.kt`: dropped `CorridorEntity.toDomain()`.
- `RewardComposer.kt`: dropped `composeCorridorReward` (only caller was `CorridorOrienteer`);
  kept `composeEdgeReward` — pre-existing dead code with zero production callers even before this
  change, so out of scope here (same reasoning #152 applied to `HazardFilter`: don't fold an
  unrelated stale-code finding into this removal's diff).
- `CyclingAssetDatabase.kt`: dropped `MapTurnEntity`/`CorridorEntity`/`CorridorConnectorEntity`
  from the `@Database` entity list and the `mapTurnDao()`/`corridorDao()` accessors, version 6 ->
  7. `DatabaseModule.kt`: dropped the matching `@Provides` methods and the `turnDao`/`corridorDao`
  constructor params on `MapGraphRepositoryImpl`.
- `Constants.kt`: dropped `ROUTE_START_RADIUS_M`, the three `FAST_WAY_HOME_GPS_*`/`BBOX_MARGIN_M`
  constants, `FAST_WAY_HOME_TRACK_WIDTH`, and every `PLAN_A_RIDE_*` constant. Kept
  `FAST_WAY_HOME_TRACK_COLOR` — still load-bearing for the unrelated "POIs along .gpx" orange
  segment highlight in `MapViewScreen.kt`, caught only by grepping its actual call sites rather
  than trusting the name.
- `MapViewScreen.kt`: removed the `fastWayHomeViewModel`/`planARideViewModel` params, their state
  collection, the Fast-Way-Home/Plan-a-Ride map-render `LaunchedEffect`s, both result cards, both
  FABs, the "Plan a ride" distance/direction/explore-weight dialog, and now-dead icon imports
  (`DirectionsBike`, `Home`) — left every unrelated composable (GPX overlay, POI layer, interval
  overlay, layers panel) untouched.

**Explicitly kept, re-confirmed**: `GpxExporter.kt` (still used by `RepeatedRouteDetailViewModel`'s
export) and `MapMatcher.kt` (still used by `IntervalClusteringService`) — grepped fresh rather
than trusting #152's PRD text, since that PRD is itself still unexecuted in this codebase.
`HazardFilter` was already gone (commit `1c133da`, prior session). `IntervalClusteringService`/
`MapMatcher` untouched apart from one doc-comment edit (`MapMatcher.kt`'s class-doc no longer
name-drops the now-deleted `FastWayHomeService`).

**Risk flagged and verified, not assumed**: bumping `CyclingAssetDatabase`'s version while it uses
`createFromAsset` + `fallbackToDestructiveMigration` could in theory either hard-crash (identity-
hash mismatch) or silently wipe the bundled graph (destructive fallback) if the checked-in asset's
baked-in schema no longer matches the narrowed entity list. Ran the real fixture-contract test
(`CyclingAssetDatabaseFixtureTest`, from #124) against the actual checked-in
`cycling_graph_fixture.db` rather than assuming either way — it opened clean with real data
intact, no destructive wipe, so no fixture regeneration was needed for this change. Left a note
in that test's docstring recording what was verified, for whoever does the `velometrics.db` asset
swap (#156) next.

### Tests
Deleted the dedicated test files for every removed production file (`RouteGeneratorTest`,
`CorridorOrienteerTest`, `RouteShapeGateRegressionTest`, `ExitLegPlannerTest`, `LoopHullTest`,
`RouteRefinerTest`, `JunctionCostTest`, `RouteShapeDiagnosisHarness`, `RouteShapeMetricsTest`,
`DegradationPolicyTest`, `FastWayHomeServiceTest`, `MapTurnEntityTest`,
`CorridorConnectorEntityTest`) plus `EntityMappersTest` (its only case, `CorridorEntity.toDomain`,
had no surviving subject). Trimmed `RewardComposerTest` to drop its corridor-reward cases only.
Stripped now-nonexistent-interface overrides (`getTurnsNear`/`getAllCorridors`/etc.) from the fake
`MapGraphRepository` test doubles in `MapMatcherTest`/`MapViewViewModelTest`, and the two
route-search-specific assertions from `CyclingAssetDatabaseFixtureTest` (kept its core
no-destructive-wipe/schema-mismatch assertions).
`compileDebugKotlin`, `compileDebugUnitTestKotlin`, `assembleDebug`, full `testDebugUnitTest`:
all pass clean (281 tests, 0 failures/errors) — no dangling references, no regressions. No
on-device verification this session (mechanical removal + fixture-test-verified DB path; the
replacement UX now lives in Ride-Graph's browser app, not this repo).

---

## Issue #153 — Ride Session Detail: vs. last five rides and vs. all previous rides
Completed: 2026-08-02

### What was done

Full `/grill-me` design session before implementing (issue was terse: two triangles instead of
arrows, colors specified, a legend, and an unresolved either/or about whether a "close to
reference" state should still exist). Resolved: fully replace `MetricCell`'s existing single-arrow
comparison (only `SessionDetailScreen` exercised it — `RepeatedIntervalDetailScreen` uses
`MetricCell` but never passed `median`, so it was unaffected); strictly binary up/down, no
dead-band "same" state; colors overridden by the maintainer away from the issue's suggested
green/yellow/gray to green `0xFF4CAF50` (better, matching the pre-existing arrow's green so at
least one color carries over) and `#592B0A` (worse) with no third/neutral color, since every
metric on this screen already declares a `higherIsBetter` direction; all 10 comparable metric
cells get both triangles (the 3 that previously had no comparison — Calories, Elevation gain,
Elev./100km — got wired up too); "Fat / Carbs" stays without triangles since it renders two
numbers as one string with no single scalar to compare; median (not the issue's literal "average")
for "all previous," for consistency with the pre-existing last-5 logic; each triangle gates
independently on its own ≥2-sample pool rather than an all-or-nothing overall gate (matters
mainly for power-derived metrics, where the last-5 pool's power-having subset can differ in size
from the all-previous pool's); triangle glyphs are Material's built-in `ArrowDropUp`/`ArrowDropDown`
(genuinely triangular, not the old arrow glyphs) rather than a custom-drawn shape; legend is a
static row bottom-aligned with the date header, not a tap-to-reveal popover.

**`data/local/dao/CyclingSessionDao.kt`, `domain/repository/CyclingSessionRepository.kt`,
`data/repository/CyclingSessionRepositoryImpl.kt`, test `FakeCyclingSessionRepository.kt`**
- New `getAllSessionsBeforeDate(epochMs)`: same `WHERE sessionStart < :beforeEpochMs` shape as the
  existing `getSessionsBeforeDate(epochMs, limit)`, just without the `LIMIT` — no shared/cached
  singleton needed (per the maintainer, ad-hoc per-screen-load computation is fine at this app's
  scale, matching how the existing last-5 query already works).

**`domain/service/SessionComparator.kt`**
- `computeComparison` now always returns a non-null `SessionComparison` (previously null outright
  below 2 total previous sessions) — every median field is independently nullable instead, so a
  metric with an insufficient sub-population (e.g. no power-having rides yet) simply omits its own
  triangle rather than suppressing the whole card's comparisons.
- Extracted the existing per-list median logic into a private `computeMedians(sessions)` helper,
  called once for the last-5 pool and once for the (new) all-previous pool, producing paired
  `medianXLast5`/`medianXAllPrevious` fields for 10 metrics (duration, distance, speed, avg power,
  normalized power, fat efficiency, cardiac efficiency, plus 3 new ones: total kcal via
  `SessionEnergy.from`, elevation gain, and elevation gain per 100km computed the same way
  `AllTimeStatsAggregator` does it). `medianIntervalTimeSec` (last-5 only) is untouched — it's
  already dead code today (nothing on the screen reads it), left as-is rather than expanding an
  unused field to a second pool.
- The ≥2-sample gate moved from a single early-return guard into the shared `median()` helper
  itself (returns null below 2 values), so it now applies independently per metric per pool for
  free, without per-field threshold logic scattered through `computeComparison`.

**`ui/components/MetricCell.kt`**
- Replaced the single `median`/arrow parameter with `medianLast5`/`medianAllPrevious`, each
  rendering its own `getTriangle()` result (or nothing) as a 14dp `Icon` right after the value, in
  that left-to-right order. `getTriangle` returns null (no icon) on an exact tie between `current`
  and the reference — the binary-only redesign has no visual for "unchanged," so a literal tie
  (only realistically possible for `Int`-valued medians) renders as neither triangle rather than
  guessing a direction.

**`ui/screens/sessiondetail/SessionDetailScreen.kt`**
- All 10 in-scope `MetricCell` calls updated to the new dual-parameter API; Calories/Elevation
  gain/Elev.-per-100km gained real `current` values (previously always passed `null`) alongside
  their new median pairs.
- New private `ComparisonLegend()`: two neutral-tinted `ArrowDropUp` icons labeled "Last 5" / "All
  previous", placed in the same `Row` as the date header via `Arrangement.SpaceBetween` +
  `Alignment.Bottom` so the legend text's baseline sits flush with the date title's baseline, per
  the maintainer's explicit layout instruction.

### Tests
`SessionComparatorTest` rewritten for the new always-non-null return shape and renamed fields: the
old "returns null" tests became "yields null medians but reports the count" tests; existing
3/4-previous-session cases updated to assert both pools (identical when total previous sessions
<= 5); one new case with 7 previous sessions constructed so the last-5 and all-previous medians
provably diverge (42.0 vs. 41.0 on a deliberately skewed distance fixture), covering the "pools
differ once ride count exceeds 5" behavior the grill-me session called out. `compileDebugKotlin`,
`assembleDebug`, full `testDebugUnitTest --rerun-tasks` (matching the known Gradle env-var/
test-filter stale-cache quirk from #135): all pass, no regressions. No on-device verification this
session (Compose-only visual change, no new libraries/permissions; the maintainer specified exact
colors/icons/layout during the grill-me session rather than leaving open questions that would
need visual confirmation).

---

## Issue #154 — Cardiac efficiency: Diagramm (Cardiac Drift chart)
Completed: 2026-08-02

### What was done

Only `ready-for-agent` issue in the queue. The issue itself was two open questions (bucket size,
low-power handling); the maintainer had already resolved the full design via a `/grill-me` session
posted as a comment before this session started, so this was implementation against that finalized
plan rather than a fresh design pass.

One genuine ambiguity remained in the plan text: "following the Fat Efficiency pattern — no schema
change, no persisted raw time series." Fat Efficiency's own pattern *does* persist its output via new
nullable columns (a real migration), and nothing else in the schema could hold per-bucket drift data
for the chart to read back on a later visit to Session Detail. Asked the maintainer directly rather
than guessing: confirmed "no schema change" was shorthand for "no new table, no raw per-sample
series" (contrasted against a rejected alternative of persisting the full per-second stream), not
"literally zero new columns" — so this adds two new nullable columns exactly like
`fatEfficiencyHistogram`/`fatEfficiencyScore` did.

**`util/Constants.kt`**
- `CARDIAC_DRIFT_BUCKET_SEC` (600), `CARDIAC_DRIFT_MIN_RIDE_SEC` (3600), the 25%-FTP exclusion
  fraction, and the 50% bucket-drop fraction.

**`domain/model/CardiacDriftBand.kt`** (new)
- `GOOD`/`NORMAL`/`SIGNIFICANT` enum with `fromPercent(decouplingPercent)`, boundaries at 5%/10%
  matching the plan's classic-Coggan bands.

**`data/fitimport/SessionMetricsCalculator.kt`**
- New `computeCardiacDrift`: splits the ride into fixed 600-second buckets (a trailing partial
  bucket is absorbed into the last full bucket via `coerceIn(0, bucketCount - 1)` on the bucket
  index, rather than kept as its own short bucket); per bucket, samples with raw power below 25% FTP
  are excluded from that bucket's power/HR averages (deliberately using raw `dp.power`, not the
  effective/substituted power used for fat-burn calculations elsewhere in this file — using the
  substituted value would defeat the exclusion, since coasting/descending streaks get backfilled
  with a plausible non-zero wattage there); a bucket is dropped (absent from the result map) if more
  than half its samples were excluded this way. Baseline = avg EF (power/HR) over first-half buckets
  (by bucket index, `bucketCount / 2`); decoupling % = decline from that baseline to the second-half
  average. Returns null outright if `totalDurationSec < 3600` (the 60-minute/6-bucket gate lives
  here, not in the UI) or if either half ends up with zero surviving buckets. Only invoked when
  `hasPower && avgHeartRate != null`, matching the existing gating convention for `fatEfficiencyScore`
  etc.

**`domain/model/CyclingSession.kt`, `data/local/entity/CyclingSessionEntity.kt`, `util/EntityMappers.kt`**
- New `cardiacDriftBuckets: Map<String, Double>?` (bucket index as String key -> % of first-half
  baseline EF; a dropped bucket is simply absent, not a zero) and `cardiacDriftPercent: Double?`.
  JSON (de)serialization follows the exact `hrZoneDistribution`/`fatEfficiencyHistogram` pattern, with
  a separate `Map<String, Double>` `TypeToken` alongside the existing `Map<String, Int>` one (the other
  histograms are all integer counts; this one needs fractional precision).

**`data/local/VelometricsDatabase.kt`, `di/DatabaseModule.kt`**
- DB version 12 -> 13. `MIGRATION_12_13`: two plain `ALTER TABLE ADD COLUMN` statements, no data
  transform needed (purely additive, both nullable) — same shape as `MIGRATION_8_9`/`MIGRATION_9_10`,
  which also didn't get a dedicated migration test for the same reason (nothing to get wrong beyond
  what Room's own schema validation already catches).

**`ui/components/CardiacDriftChart.kt`** (new)
- Line chart (a deliberate departure from this screen's existing bar-chart idiom, per the plan) — one
  Canvas per session, plotting each surviving bucket's % of baseline with a dashed 100% reference
  line. Consecutive present buckets are connected; a dropped bucket (missing from the map) breaks the
  line rather than being interpolated across or drawn as zero, implemented by only drawing a segment
  between `i` and `i+1` when both are non-null. Header shows the decoupling % and a
  Good/Normal/"Significant drift" label colored by `CardiacDriftBand` (green/orange/red, same visual
  language as `FatEfficiencyHistogram`'s bin colors, reordered since here green means *low* drift).
  X-axis labels are minute marks, stride-limited to ~6 labels so long rides with many buckets don't
  crowd the axis.

**`ui/screens/sessiondetail/SessionDetailScreen.kt`**
- New card inserted directly after `HeartRateZoneChart`, gated on
  `s.cardiacDriftBuckets != null && s.cardiacDriftPercent != null` — since the calculator already
  folds every other gate (ride length, power+HR presence, both-halves-have-data) into whether it
  returns null, this single non-null check is sufficient and there's no separate UI-side gating logic
  to keep in sync. No `ViewModel` changes needed; both fields ride along on the existing `session`
  `StateFlow` the same way `fatEfficiencyHistogram` does.

### Known gap
Like the `BestEffort`/power-curve data from #143/#145, `cardiacDriftBuckets`/`cardiacDriftPercent`
are computed only at import time from the raw per-record stream, which `FitImportService` discards
afterward — none of the existing 148 already-imported rides will show this card unless re-imported.
No backfill; accepted gap, consistent with the established precedent for this exact class of problem.

### Manual verification (on-device)
Installed the debug APK over the real 148-ride production database and launched it: confirmed via
`adb logcat` that the app started cleanly with no crash and no `FATAL`/`AndroidRuntime` entries,
i.e. `MIGRATION_12_13` applied successfully against real data. Could not go further — the device's
lock screen requires biometric/PIN entry that isn't available to drive non-interactively, so the
actual chart rendering (which needs a fresh post-migration import of a real >=60-minute ride with
both power and HR) was not visually confirmed this session. Verified by code review and the unit
test suite instead.

### Tests
5 new `SessionMetricsCalculatorTest` cases: below the 60-minute gate returns null; missing power or
missing HR each return null independently; a constant-EF ride reads every bucket at exactly 100% of
baseline with 0% decoupling; a synthetic ride with a real second-half power drop (250W->150W at
constant HR) produces the hand-calculated 40% decoupling and matching per-bucket percentages; a
bucket with 6 of 10 samples below the 25%-FTP threshold is dropped entirely (absent from the map)
rather than averaged over just the 4 survivors. `compileDebugKotlin` (confirms the v13 Room schema/
migration resolves and exports `13.json`), `compileDebugUnitTestKotlin`, full `testDebugUnitTest
--rerun-tasks` (matching the known Gradle env-var/test-filter stale-cache quirk from #135): all pass
(12/12 in `SessionMetricsCalculatorTest`), no regressions.

---

## Issue #151 — Ride detail: Speed distribution card is wider than the other cards
Completed: 2026-07-29

### What was done

Issue had no body/labels (picked directly per the user's explicit request, not from the
`ready-for-agent` queue — none tagged at session start). Root cause: `SpeedHistogramChartContent`'s
`Card` used a bare `Modifier.fillMaxWidth()`, unlike every sibling card on `SessionDetailScreen`
(`PowerZoneChart`, `HeartRateZoneChart`, `FatEfficiencyHistogram`, `SprintCard`, `IntervalListCard`),
which all bake `Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 8.dp)` directly into
their own `Card` — that's how this screen achieves consistent card margins, since `PullUpDrawer`'s
content column applies no horizontal padding of its own (each card owns its margin). The Speed
Distribution card (added in #148) missed this convention, rendering edge-to-edge and visibly wider
than its siblings.

Complication: `SpeedHistogramChartContent` is shared with `SpeedHistogramChartAvg`, used on
`RepeatedRouteDetailScreen` inside a `Column(modifier = Modifier.padding(16.dp), ...)` that already
provides margin — that screen's own `Card(modifier = Modifier.fillMaxWidth())` usages (stats/duration
estimator cards) rely on the parent column's padding and add none of their own. Simply adding the
16dp horizontal padding inside the shared Card would have double-padded that screen's card instead
(32dp inset vs. 16dp for its siblings), just moving the same bug elsewhere.

**`ui/components/SpeedHistogramChart.kt`**
- `SpeedHistogramChartContent` gained a `cardModifier: Modifier` parameter, applied to the `Card`
  instead of a hardcoded `Modifier.fillMaxWidth()`.
- `SpeedHistogramChart` (SessionDetailScreen) passes
  `Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 8.dp)` — matching its siblings.
- `SpeedHistogramChartAvg` (RepeatedRouteDetailScreen) passes plain `Modifier.fillMaxWidth()` —
  unchanged behavior, since that screen's parent column already pads.

### Tests
No new tests (pure `Modifier` plumbing, no logic change — verified by inspection against the other
five card composables' modifier convention on both screens). `compileDebugKotlin`,
`compileDebugUnitTestKotlin`, full `testDebugUnitTest --rerun-tasks` (matching the known Gradle
env-var/test-filter stale-cache quirk from #135): all pass, no regressions. No on-device
verification this session (layout-only Modifier change, low visual risk, matches an established
pattern already proven on five other cards on the same screen).

---

## Issue #150 — Cache AllTimeStatsViewModel aggregates instead of recomputing on every load
Completed: 2026-07-27

### What was done

Only `ready-for-agent` issue in the queue; a direct follow-up to #147, which introduced the
`GlobalAverageCache` reactive-`StateFlow` pattern and filed this issue as the natural next
application of it. `AllTimeStatsViewModel` previously recomputed the entire all-time-best trio,
distance splits, power curve, per-year breakdown, and power/speed point cloud from scratch on every
screen load (`sessionRepository.getAllSessions().map { buildUiState(...) }`), including an eager
`bestEffortRepository.getAllWithSessionDate()` call inside that `map`. Applied the same
computation/cache split #147 established for `GlobalAverageCalculator`/`GlobalAverageCache`: pulled
the pure aggregation logic out into a standalone object with no ViewModel/coroutine dependencies,
then wrapped it in a singleton, app-scoped hot `StateFlow` cache, eagerly registered in
`VelometricsApplication` so computation starts at process launch rather than on first navigation.

One wrinkle `GlobalAverageCache` didn't have to deal with: this screen combines two sources
(`cycling_sessions` and `session_best_efforts`), and `FitImportService` writes them in two separate
suspend calls — session insert first, best-effort insert second (not one transaction). A cache built
by just hoisting `bestEffortRepository`'s old suspend `getAllWithSessionDate()` into a `.map` off the
sessions Flow would only ever see best-effort data current as of whenever the cache happened to
recompute, with no trigger to re-run after the second write landed. Fixed by changing
`SessionBestEffortDao.getAllWithSessionDate()` from a suspend one-shot query to a Room `Flow`-returning
query — since its SQL joins `session_best_efforts` against `cycling_sessions`, Room's invalidation
tracker re-runs it on a write to *either* table, so `combine(sessions, bestEfforts)` naturally settles
on consistent data shortly after both writes land, without any manual invalidation wiring (same
"no manual invalidation" property #147 relied on).

**`domain/model/AllTimeStatsUiState.kt`** (new)
- `RecordEntry`, `PowerCurvePoint`, `YearStat`, `PowerSpeedPoint`, `AllTimeStatsUiState` moved here
  verbatim from the ViewModel file, so the cache (`data/cache`) can depend on them without depending
  on the `ui` package — matches `RepeatedRoutesCache`/`RepeatedIntervalsCache` returning
  domain-layer types rather than UI types.

**`domain/service/AllTimeStatsAggregator.kt`** (new)
- `buildUiState(sessions, bestEfforts): AllTimeStatsUiState`, plus every private helper
  (`longestRideEntry`, `biggestClimbEntry`, `buildYearStats`, `buildPowerSpeedPoints`,
  `percentileRange`/`percentile`, `elevationBucketFor`, `splitEntry`, `powerCurvePoint`) moved
  unchanged from the ViewModel into a plain `object`, mirroring `GlobalAverageCalculator`'s shape —
  a pure function with no coroutine/DI surface, directly unit-testable.

**`data/cache/AllTimeStatsCache.kt`** (new)
- `combine(sessionRepository.getAllSessions(), bestEffortRepository.getAllWithSessionDate()) { ... }`
  through `AllTimeStatsAggregator.buildUiState`, `.stateIn(scope, SharingStarted.Eagerly,
  AllTimeStatsUiState())` — same shape as `GlobalAverageCache`.

**`data/local/dao/SessionBestEffortDao.kt`, `domain/repository/BestEffortRepository.kt`,
`data/repository/BestEffortRepositoryImpl.kt`**
- `getAllWithSessionDate()` changed from `suspend fun (): List<BestEffortRecord>` to
  `fun (): Flow<List<BestEffortRecord>>` for the reactivity reason above. `insert` untouched.

**`ui/screens/alltimestats/AllTimeStatsViewModel.kt`**
- Reduced to `val uiState: StateFlow<AllTimeStatsUiState> = cache.uiState`, no computation left —
  matches how `RepeatedRoutesViewModel`/`RepeatedIntervalsViewModel` consume their caches.

**`ui/screens/alltimestats/AllTimeStatsScreen.kt`**
- No behavior change; added imports for the four data classes now living in `domain/model` instead
  of the same package.

**`VelometricsApplication.kt`**
- New `@Inject lateinit var allTimeStatsCache: AllTimeStatsCache` alongside the other three eagerly
  registered caches.

### Tests
Moved `AllTimeStatsViewModelTest` -> `domain/service/AllTimeStatsAggregatorTest.kt`: same 9 cases,
now calling `AllTimeStatsAggregator.buildUiState(sessions, bestEfforts)` directly instead of
constructing a ViewModel with fake repositories and collecting a `StateFlow` under
`StandardTestDispatcher` — simpler and matches `GlobalAverageCalculatorTest`'s plain-JUnit style,
since the logic is now a pure function with nothing to inject. `FakeBestEffortRepository` updated to
return `flowOf(records.toList())` for the new `Flow`-returning signature. No dedicated
`AllTimeStatsViewModelTest`/`AllTimeStatsCacheTest` added — no other cache-consuming ViewModel or
cache in this codebase (`RepeatedRoutesCache`, `RepeatedIntervalsCache`, `GlobalAverageCache`) has
one either; the pass-through wiring has nothing left to assert beyond what compilation already
guarantees. `compileDebugKotlin`, `compileDebugUnitTestKotlin`, full `testDebugUnitTest
--rerun-tasks` (matching the known Gradle env-var/test-filter stale-cache quirk from #135): all
pass, no regressions.

---

## Issue #148 — Speed distribution: changes
Completed: 2026-07-27

### What was done

Full /grill-me design session before implementing (issue was terse: new buckets plus "the ride
session Detail Screen should also receive a Speed distribution card"). Resolved: historical-data
handling (the new 5-bucket scheme is an exact coarsening of the old 8-bucket one — every new
boundary lines up with an old one — so a real Room migration merges existing rows' counts instead
of relying on `fallbackToDestructiveMigration`, which would have silently wiped every ride on
upgrade), scope of the bucket change (global — `CyclingConstants.SPEED_HISTOGRAM_BINS` is a single
shared constant, so RepeatedRouteDetail's existing average card picks up the new buckets too, not
just the new per-ride card), card placement (SessionDetailScreen, right after Power Zones, always
shown since speed comes from GPS not the power meter), percentage-calc layering (in
SessionDetailViewModel, mirroring RepeatedRouteDetailViewModel's existing count-to-percentage
convention rather than inside the Composable), and test scope (only the migration — the
count-to-percentage math is simple enough to skip dedicated coverage, but the migration runs once
against real persisted ride data and a bug there is silent and hard to recover from).

Mid-session (post #147, which had already landed on `main` an all-rides average reference line for
Power/HR zone charts on this same screen and a `GlobalAverageCache.speedHistogramAverages`), wired
the new card to that same cache for consistency with its Power Zones/HR Zones siblings, rather than
leaving it as the only distribution card on the screen without a reference tick.

**`util/Constants.kt`**
- `SPEED_HISTOGRAM_BINS` narrowed from 8 buckets (0-5, 5-10, 10-20, 20-25, 25-30, 30-35, 35-40, >40)
  to 5 (0-10, 10-20, 20-30, 30-40, >40), per the issue.

**`data/local/VelometricsDatabase.kt`, `di/DatabaseModule.kt`**
- DB version 11 -> 12. New `MIGRATION_11_12` (made `internal`, not `private`, so
  `VelometricsMigrationTest` can reference it directly): reads every `cycling_sessions.speedHistogram`
  row's JSON, sums old-bucket counts into the new 5 labels via a fixed old-label -> new-label map
  (`"0-5 km/h"`/`"5-10 km/h"` both -> `"0-10 km/h"`, `"10-20 km/h"` unchanged, etc.), and writes the
  merged JSON back. No raw datapoints needed (`FitImportService` discards those after import) since
  the merge only needs the already-aggregated old counts.

**`ui/components/SpeedHistogramChart.kt`**
- `barColors`/`shortLabels` cut from 8 to 5 entries (yellow-to-red gradient over the new buckets).
- New `SpeedHistogramChart(percentages, allRidesAveragePercentages)` composable for a single ride,
  delegating to the existing private `SpeedHistogramChartContent` (same rendering, including the
  #147 average-tick-mark behavior) that `SpeedHistogramChartAvg` already used for the RepeatedRoute
  average card.

**`ui/screens/sessiondetail/SessionDetailViewModel.kt`**
- New `speedHistogram: StateFlow<Map<String, Float>>`: this ride's own bucket counts converted to
  percentage-of-total, computed from `session` the same way `RepeatedRouteDetailViewModel` already
  converts counts to percentages per bin.
- New `speedHistogramAverages: StateFlow<Map<String, Float>>` sourced directly from
  `GlobalAverageCache`, mirroring the existing `powerZoneAverages`/`hrZoneAverages` wiring.

**`ui/screens/sessiondetail/SessionDetailScreen.kt`**
- New `SpeedHistogramChart` call inserted right after the Power Zones card/placeholder block and
  before Heart Rate Zones, always rendered (no `hasPower`/null gating, unlike Power Zones).

**`app/build.gradle.kts`**
- New `sourceSets { getByName("debug").assets.srcDirs("$projectDir/schemas") }` (plus `androidTest`
  for parity) so `MigrationTestHelper` can find the Room-exported schema JSONs. First attempt added
  this to the `test` source set instead, which compiled but failed at runtime
  (`FileNotFoundException: ... Missing file: .../11.json`) — Robolectric's local unit tests read
  assets from the `debug` variant's merged-assets output (`isIncludeAndroidResources`), not a
  `test`-scoped source set; switching to `debug` (rather than `main`) also keeps the schema JSONs
  out of release builds.

### Tests
New `VelometricsMigrationTest` (`MigrationTestHelper` + Robolectric, `@SQLiteMode(NATIVE)`,
`@Config(application = Application::class)` matching `CyclingAssetDatabaseFixtureTest`'s pattern):
seeds a v11 row with all 8 old buckets populated with distinct counts, runs
`DatabaseModule.MIGRATION_11_12`, and asserts each new bucket equals the exact sum of its
contributing old buckets with no extra/missing keys. Per the grill-me scope decision, no new tests
for the calculator/ViewModel percentage math (verified by inspection).
`compileDebugKotlin`, `compileDebugUnitTestKotlin`, full `testDebugUnitTest --rerun-tasks` (matching
the known Gradle env-var/test-filter stale-cache quirk from #135): all pass (467 tests), no
regressions. No manual on-device verification this session (outside this issue's explicit scope).

---

## Issue #149 — All-time Stats: point clouds diagram
Completed: 2026-07-27

### What was done

Full /grill-me design session before implementing (issue was terse: 5 bullet points, no
elaboration). Resolved: scope (always all-time, ignores the year-navigation selector added in a
prior session — the point cloud's whole appeal is showing every ride at once), missing-data
handling (silently exclude rides with null `averagePower` or null `elevationGainM`, no count
shown), speed formula (`distanceKm / netDurationSec * 3600`, matching
`RepeatedRouteDetailViewModel`'s existing convention), component reuse (extend the existing
`ScatterPlotChart`/`ScatterPoint` rather than write a bespoke chart — added an optional per-point
color and a configurable dot radius, both backward-compatible defaults so
`RepeatedRouteDetailScreen`'s existing usage is untouched), axis-step logic (extracted
`RepeatedRouteDetailScreen`'s inline floor/ceil-to-nearest-10 X-axis rounding into a shared
`roundedAxisBounds` util, now used by both screens — Y axis stays auto-padded, matching the
existing "Avg Power vs Speed" chart's own behavior, since the issue's "reuse X-/Y-axis step logic"
turned out to describe that whole existing chart, not a new Y-axis rounding rule), dot size (2dp,
vs. 6dp default), color scheme (5-step alpha ramp on `colorScheme.primary` rather than 5 hardcoded
hex swatches like the zone charts use, so it adapts to theme/dynamic color same as
`PowerCurveSection` on the same screen), a compact scrollable legend row, no tap interaction
(matches `ScatterPlotChart`'s existing static-only behavior), and no new unit tests (treated as a
UI-only addition, verified on-device).

**`ui/components/ScatterPlotChart.kt`**
- `ScatterPoint` gains an optional `color: Color? = null`, falling back to the existing single
  `dotColor` when absent.
- New `dotRadius: Dp = 6.dp` parameter on `ScatterPlotChart`.
- New top-level `roundedAxisBounds(min, max, step = 10f)`, extracted from
  `RepeatedRouteDetailScreen`'s inline calculation.

**`ui/screens/repeatedroutedetail/RepeatedRouteDetailScreen.kt`**
- Both "Avg Power vs Speed" and "Normalized Power vs Speed" scatter charts now call
  `roundedAxisBounds` instead of duplicating the floor/ceil rounding inline. No behavior change
  (verified on-device: dot size, color, and axis ticks all unchanged).

**`ui/screens/alltimestats/AllTimeStatsViewModel.kt`**
- New `PowerSpeedPoint(avgPowerW, avgSpeedKmh, elevationBucket)` and
  `AllTimeStatsUiState.powerSpeedPoints`.
- `buildPowerSpeedPoints`: filters to sessions with `hasPower`, non-null
  `averagePower`/`elevationGainM`, and positive `netDurationSec`/`distanceKm`; computes speed the
  same way as `RepeatedRouteDetailViewModel`, and buckets `elevationGainM / distanceKm * 100` into
  5 lower-inclusive ranges (0-200, 200-800, 800-1400, 1400-2000, >2000) via `elevationBucketFor`.

**`ui/screens/alltimestats/AllTimeStatsScreen.kt`**
- New `PowerSpeedPointCloudSection`, inserted after `PowerCurveSection` and before
  `YearBreakdownSection`, titled "Power vs. Speed". Always shows the Card (matching
  `PowerCurveSection`'s convention); shows an empty-state message instead of hiding entirely when
  `powerSpeedPoints` is empty.
- 5-shade legend (`ElevationBucketLegend`) in a horizontally-scrollable row below the chart, since
  the 5 range labels don't all fit on narrow screens at once.

### Manual verification (on-device)
Installed the debug APK and navigated to All-time Stats via Home's overflow menu. Confirmed the
new "Power vs. Speed" section renders between Power curve and By year, with small (2dp) dots
clearly smaller than the RepeatedRoutes chart's dots, X-axis step-rounded to the nearest 10
(matching the underlying data), Y-axis auto-padded, and a legend row that scrolls to reveal the
">2,000" label cut off at rest (confirmed this is the intended scroll affordance, not a clipping
bug, by swiping it into view). Verified with two datasets on the same device: an initial 7-ride set
(3 rides had both power and elevation data, correctly excluding the other 4) and, after the
maintainer imported more rides mid-session, a ~148-ride set showing a dense, clearly-trending
cloud. Regression-checked `RepeatedRouteDetailScreen`'s own two scatter charts (which share the
modified `ScatterPlotChart`/`roundedAxisBounds` code) — both still render with their original
larger dot size, single color, and correct axis ticks.

### Tests
No new automated tests (UI-only addition, per plan — verified on-device instead).
`compileDebugKotlin`, `compileDebugUnitTestKotlin`, full `testDebugUnitTest --rerun-tasks`
(matching the known Gradle env-var/test-filter stale-cache quirk from #135): all pass, no
regressions.

---

## Issue #147 — Power/HR zone and speed distribution: all-rides average reference line
Completed: 2026-07-27

### What was done

Full /grill-me design session before implementing (issue was open-ended: "I want to calculate an
overall average over all rides... displayed in the diagrams as a thin horizontal line"). Resolved:
scope (global average shown on session-level Power/HR zone charts, plus a second reference line on
the RepeatedRoute speed histogram alongside its existing per-route average), equal-weight
normalization (average of each ride's own percentage-of-time per zone/bin, not raw seconds — a
20-minute and a 5-hour ride count equally, matching the existing per-route speed histogram average
pattern), accepted the FTP/max-HR-drift limitation (zone boundaries are baked in per-ride at import
time and raw streams aren't retained, so the average is computed over stored zone data as-is),
missing-data handling (a ride without power or HR data is skipped only for that specific average),
and the tick-mark visual (independent per-bar tick, not one continuous line, since each zone/bin
has its own average value).

Mid-session, the interview surfaced a better-fitting caching approach than the one first agreed:
this codebase already has `RepeatedRoutesCache`/`RepeatedIntervalsCache` (`data/cache/`), an
app-scoped hot `StateFlow` pattern that auto-refreshes whenever the underlying Room `Flow` emits.
Switched the plan from a DataStore-persisted cache with manual recompute-after-import/delete calls
(which would have needed a circular-dependency workaround) to the same reactive in-memory pattern
instead.

**`domain/service/GlobalAverageCalculator.kt`** (new)
- `computePowerZoneAverages`/`computeHrZoneAverages`/`computeSpeedHistogramAverages`: each ride's
  distribution is normalized to percentage-of-that-ride's-time per label first, then percentages
  are averaged equally across rides (equal weight regardless of ride length). Rides missing a given
  distribution (`powerZoneDistribution`/`hrZoneDistribution` null) are excluded from that specific
  average via `mapNotNull`, but `speedHistogram` is never null so every ride always contributes to
  that one.

**`data/cache/GlobalAverageCache.kt`** (new)
- Same shape as `RepeatedRoutesCache`: hoists `CyclingSessionRepository.getAllSessions()` into an
  app-scoped `StateFlow`, with the three averages as derived `StateFlow`s via
  `GlobalAverageCalculator`. Auto-refreshes on any DB change (import, delete) with no manual
  invalidation wiring — sidesteps a circular-DI issue a DataStore-backed approach would have hit (a
  repository depending on `CyclingSessionRepository` for reads, while
  `CyclingSessionRepositoryImpl.deleteSession` would need to call back into it).
- Registered eagerly in `VelometricsApplication` (same `@Inject lateinit var` pattern as the other
  two caches) so `SharingStarted.Eagerly` collection starts at process launch.

**`ui/components/PowerZoneChart.kt`, `HeartRateZoneChart.kt`, `SpeedHistogramChart.kt`**
- New `averagePercentages`/`allRidesAveragePercentages` parameter (default `emptyMap()`, backward
  compatible). Each bar's `Canvas` changed from a variable-height canvas (the old trick of sizing
  the Canvas itself to the bar's height) to a fixed-height canvas that manually draws a
  bottom-aligned bar rect plus an independent tick mark per bar — needed because a single average
  value can exceed what a specific ride/route's own chart would otherwise scale to. The shared
  per-chart max (`maxPct`) is computed across both the bar values and the average values so a tick
  is never clipped off the top. Tick is drawn as two overlaid `drawLine`s (a wider
  `colorScheme.surface` outline behind a narrower `colorScheme.onSurface` line) so it reads over any
  zone/bin bar color in either theme. Preserved the original 2dp minimum-visible-bar-height floor
  that the fixed-height restructuring would otherwise have dropped.

**`ui/screens/sessiondetail/SessionDetailViewModel.kt`/`Screen.kt`**
- New `powerZoneAverages`/`hrZoneAverages` `StateFlow`s sourced directly from `GlobalAverageCache`,
  collected and passed into the two zone chart composables.

**`ui/screens/repeatedroutedetail/RepeatedRouteDetailViewModel.kt`/`Screen.kt`**
- `uiState` now built via `combine(repository.getRouteById(routeId),
  globalAverageCache.speedHistogramAverages)` instead of a single `.map`; new
  `allRidesSpeedHistogram` field passed through to `SpeedHistogramChartAvg`.

Filed #150 as a related follow-up (maintainer requested during the design session): apply the same
`GlobalAverageCache`-style reactive caching to `AllTimeStatsViewModel`'s existing aggregates, which
currently recompute on every screen load — `ready-for-agent`, depends on this issue's pattern.

### Manual verification (on-device, real production data — 148 rides)
Built and installed the debug APK on a connected device. Confirmed on a real ride's detail screen:
Power Zones and Heart Rate Zones charts both show a contrasting tick mark on every bar (including
zones where this ride shows a small or zero percentage but the all-rides average is higher — e.g.
HR Zone 1/4/5 at 0% still show a tick above the bar, proving the shared-max scaling works).
Confirmed on a 9-session RepeatedRoute's detail screen: Speed Distribution shows ticks on every
bin, including near-empty bins, alongside the route's own pre-existing average bars.

### Tests
New `GlobalAverageCalculatorTest`: equal-weight-per-ride-length normalization (a 10-minute and
10-hour ride's zone percentages are averaged, not duration-weighted), per-metric skip-on-missing-data
(a ride without power data doesn't dilute the HR or speed averages, and vice versa), empty-input and
no-matching-data edge cases, and a zone present in only some rides is still averaged against the
full ride count (not just the rides that reached it). One iteration needed on the "skip per-metric"
test: `speedHistogram` is never null on a real session, so a fixture using `emptyMap()` as a
stand-in for "no data" was actually counted as a real (degenerate 0%) contribution — fixed by giving
both fixture sessions real speed data. `compileDebugKotlin`, `compileDebugUnitTestKotlin`, full
`testDebugUnitTest --rerun-tasks` (matching the known Gradle env-var/test-filter stale-cache quirk
from #135): all pass, no regressions.

---

## Issue #146 — All-time Stats: test coverage for BestEffortCalculator + AllTimeStatsViewModel (#143 follow-up)
Completed: 2026-07-26

### What was done

Only `ready-for-agent` issue in the queue. Follow-up to #143/#145: the data layer and screen UI
both shipped without unit coverage (scope was cut short each session) — this lands the test
coverage called out in the issue, no production code changes.

**`domain/service/BestEffortCalculatorTest.kt`** (new)
- Direct tests against the `internal` `bestTimeForDistance`/`bestAvgPowerForDuration` two-pointer
  functions using hand-built `DoubleArray` fixtures (no need to round-trip through `Datapoint`/
  haversine for these): exact match at a sample boundary, interpolated mid-segment match, ride
  shorter than target (null), target reachable only right at the very end of the ride (only the
  whole-ride window clears it), and a case with multiple candidate windows where a later fast burst
  beats an earlier slow window (regression guard on the two-pointer's window-tracking, since a naive
  implementation could get stuck returning the first window found rather than the fastest).
- `bestAvgPowerForDuration`: constant power returns that same wattage regardless of window position,
  a short high-power burst inside a longer low-power ride is correctly found (400W burst beats the
  surrounding 100W), ride shorter than duration returns null, duration exactly matching ride length
  resolves via the single whole-ride window.
- `compute`: power fields are all null when `hasPower = false` even when some `Datapoint.power`
  values are non-null (simulates a GPS-only ride with stray power readings); fewer than 2 datapoints
  returns an all-null `BestEffortValues()`.
- Cross-check test: a synthetic straight-line-north ride (pure meridian, so haversine distance is
  exactly `EARTH_RADIUS_M * dLatRadians` — no floating approximation) at a constant 10 m/s and
  constant 200W, 5 steps of 5000m/500s. Hand-calculated expectation: total distance is exactly
  25000m (the whole ride, so `split25kSec` == total elapsed time 2500s, `split50kSec`/`split100kSec`
  null since the ride never reaches them), and every power-curve bucket up to 1800s reads exactly
  200W since power is constant throughout. Exercises the full `compute()` wiring (datapoints ->
  cumulative distance/energy -> both algorithms), not just the array-level logic.

**`ui/screens/alltimestats/AllTimeStatsViewModelTest.kt`** (new)
- `yearStats` buckets sessions by calendar year via `ZoneId.systemDefault()` (mid-year instants used
  in fixtures to avoid timezone-boundary flakiness) and sorts years descending; per-year sums
  (`rideCount`/`totalDistanceKm`/`totalElevationGainM`/`totalNetDurationSec`) verified against a
  mixed multi-year session list.
- `longestRideEntry`/`longestDurationEntry` pick the correct session out of a mixed list.
- `biggestClimbEntry` ignores a session with `elevationGainM == null` even though it has the
  greatest distance of the three — proves null is excluded rather than treated as a climb of 0.
- `splitEntry`/`powerCurvePoint` pick the minimum time / maximum watts respectively across multiple
  `BestEffortRecord`s and carry through the winning record's `sessionId`/date.
- Empty-session-list and empty-best-efforts-list cases produce the expected all-null/empty
  `AllTimeStatsUiState` without throwing.
- New `FakeBestEffortRepository` test double (matching the existing `FakeCyclingSessionRepository`
  pattern) since none existed yet. `uiState` is collected via `vm.uiState.first { !it.isLoading }`
  rather than manually launching a collector job — simpler than the alternatives since
  `FakeCyclingSessionRepository.getAllSessions()` is a completed `flowOf(...)`, so the
  `WhileSubscribed(5000)`-backed `StateFlow` settles on its first real emission as soon as anything
  subscribes.

### Tests
21 new test cases (12 `BestEffortCalculatorTest`, 9 `AllTimeStatsViewModelTest`), all passing.
`compileDebugKotlin`, `compileDebugUnitTestKotlin`, full `testDebugUnitTest --rerun-tasks` (matching
the known Gradle env-var/test-filter stale-cache quirk from #135): all pass, no regressions.

---

## Issue #145 — All-time Stats: build the screen UI + navigation (#143 follow-up)
Completed: 2026-07-26

### What was done

Follow-up to #143 (data layer already on `main`) and picked over its sibling #146 (test coverage)
as higher-impact: the data layer was already shipped but completely inaccessible without this UI,
so this unlocks the whole feature rather than just hardening code nobody can reach yet. Built the
screen and navigation exactly per the design worked out with the maintainer in #143's grill-me
session.

**`ui/navigation/Screen.kt`**
- New `Screen.AllTimeStats("all_time_stats", ...)`.

**`ui/navigation/VelometricsNavHost.kt`**
- Wired `Screen.AllTimeStats.route` to a new `AllTimeStatsScreen` composable, with
  `onNavigateToSession` routing to the existing `Screen.SessionDetail`. `HomeScreen` gained an
  `onNavigateToAllTimeStats` callback wired to the new route.

**`ui/screens/home/HomeScreen.kt`**
- New overflow menu (`DropdownMenu` behind a `MoreVert` `IconButton`) on Home's `TopAppBar`, same
  pattern as `RepeatedRoutesScreen`'s sort filter menu — single "All-time stats" entry.

**`ui/screens/alltimestats/AllTimeStatsScreen.kt`** (new)
- Top-to-bottom: all-time-best trio, distance-split records, power curve, per-year breakdown — all
  consuming the existing `AllTimeStatsViewModel` with no ViewModel changes needed.
- Best-trio/distance-split records rendered as `RecordRow`s: value+date when present, an explicit
  `emptyMessage` (not a hidden section) when null; row is `clickable` only when `sessionId != null`,
  navigating to `SessionDetailScreen`.
- Power curve: custom `Canvas` line chart (hand-rolled, matching `HomeScreen`'s existing
  `MonthlyLineChart` pattern rather than a charting library), log-scale x-axis
  (`xFor(i)` maps `ln(durationSec)` linearly into chart width, since durations span 1s-30min and a
  linear axis would crush the short-duration end into a few pixels). Highlighted point defaults to
  the smallest-duration bucket that actually has data (first entry of `availableIndices`, not index
  0 of the fixed 9-bucket list, since 1s/3s/etc. can be individually null) and updates on drag via
  the same `awaitEachGesture`/`detectDragGestures`-style pointer input as `MonthlyLineChart`, snapping
  to the nearest available (non-null) point by x-distance. Gated on `hasAnyPowerCurveData`; shows
  "Import a ride with power data to see your power curve" otherwise (covers GPX-only users, per the
  issue — GPX imports never carry power).
- Per-year breakdown: initially a 4-column row (Rides/Distance/Elev. gain/Duration) matching the
  issue's stat list, but on-device testing showed "km"/"m" suffixes wrapping onto their own line at
  4-per-row width — reflowed to a 2×2 grid (Rides+Distance, then Elev. gain+Duration) which fixed it
  cleanly at the tested screen width.
- Overall empty state (`!hasAnySessions`): single centered "No rides recorded yet" message instead
  of rendering four empty sections.

### Manual verification (on-device, real production data — 148 rides)
Built and installed the debug APK on a connected device (not just Robolectric) per this project's
UI-testing convention. Confirmed: overflow menu shows "All-time stats"; navigating in shows real
all-time-best data (400.5 km longest ride, 3196 m biggest climb, 14h 7m longest duration, all with
correct dates); distance-split and power-curve sections correctly show their empty-state copy (none
of the 148 pre-#143 rides have best-effort data — the known, by-design gap since `FitImportService`
discards the raw per-record stream after import); tapping the "Longest ride" record navigates to
`SessionDetailScreen` and shows the matching 400.5 km / 5 July 2025 ride; per-year breakdown shows
2023-2026 correctly sorted most-recent-first with the fixed 2×2 grid. Did not verify the power
curve's with-data rendering/drag-to-highlight path live, since doing so needs a ride re-imported
after #143 landed and no sample `.fit` file exists in this repo or environment — verified that path
by code review only, consistent with the issue's own note that this is an accepted, known gap.

One environment note for future sessions: running `./gradlew assembleDebug` concurrently with a
still-running background `testDebugUnitTest` on Windows corrupts the packaged APK (Windows file
locking makes `transformDebugClassesWithAsm` silently fail to fully replace class outputs, producing
a `ClassNotFoundException` on `VelometricsApplication` at launch) — serialize Gradle invocations
against the same build directory rather than running them in parallel.

### Tests
No new tests added (this issue's own scope explicitly excludes UI/Compose tests, deferring to
manual on-device verification; unit test coverage for the underlying `BestEffortCalculator`/
`AllTimeStatsViewModel` logic is #146, not this issue). `compileDebugKotlin`,
`compileDebugUnitTestKotlin`, full `testDebugUnitTest --rerun-tasks` (matching the known Gradle
env-var/test-filter stale-cache quirk from #135): all pass, no regressions.
