## Issue #210 -- HomeViewModel rewired onto WorkManager-backed Dropbox sync
Completed: 2026-09-19

### What was done

- `syncDropbox()`/`autoSyncDropbox()` now `enqueueUniqueWork(DROPBOX_SYNC_WORK_NAME, KEEP)` a `DropboxSyncWorker`
  (network CONNECTED; pull-to-refresh also expedited with RUN_AS_NON_EXPEDITED fallback; `isUserInitiated` via input data).
  `DROPBOX_SYNC_WORK_NAME` lives in `DropboxSyncWorker`; `WorkModule` provides `WorkManager` to Hilt.
- `isSyncing` derives from `getWorkInfosForUniqueWorkFlow`; `dropboxSyncMessage` and the `RideReveal` branch of
  `importState` derive from `DropboxSyncOutcomeStore.outcome` and are `consume()`d on clear. Manual import unchanged.
  `HomeViewModel` no longer takes `DropboxSyncService`.
- Tests: `HomeViewModelTest` reworked (enqueue params, isSyncing, outcome consume);
  new `HomeViewModelSyncWorkTest` (real test WorkManager: second enqueue is kept out by KEEP).
- NOT done: emulator-scripted `adb shell am kill` check (no device/emulator attached this run). NeedsReauth persistence
  is covered at the worker level (`markNeedsReauth`), no new test.

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #209 — DropboxSyncWorker runs the Dropbox sync via WorkManager
Completed: 2026-09-19

### What was done

- New `data/dropbox/DropboxSyncWorker.kt` (`@HiltWorker` `CoroutineWorker`): same call sequence as
  `HomeViewModel.syncDropbox()` (connected/reauth checks, `captureBaseline`, `DropboxSyncService.sync()` reused as-is,
  `evaluate`), publishing `Reveal`/`Message` to `DropboxSyncOutcomeStore`. `isUserInitiated` is read from input `Data`
  (`KEY_IS_USER_INITIATED`, default true).
- `Completed`/`TransientFailure` -> `Result.success()` (no `retry()`); `NeedsReauth` -> `markNeedsReauth()` + `Result.failure()`.
- Recluster (route + interval, each failure logged and isolated) runs in `finally`, so on every path.
- `buildSyncMessage` moved out of `HomeViewModel` to internal `buildDropboxSyncMessage` so both share it.
  `HomeViewModel` is otherwise unchanged; wiring it to enqueue the worker is #210.
- `DropboxSyncWorkerTest` (Robolectric, `TestListenableWorkerBuilder`, mocked services, real outcome store).

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #208 — DropboxSyncOutcomeStore for persisted sync-result hand-off
Completed: 2026-09-19

### What was done

- New `data/dropbox/DropboxSyncOutcomeStore.kt` (`@Singleton`, Preferences DataStore `dropbox_sync_outcome`):
  sealed `DropboxSyncOutcome` (`Message`, `Reveal(RideRevealContent)` flattened to primitive keys),
  `publish()` (last-write-wins, clears prior keys, strictly increasing run id), `outcome` Flow, `consume()`.
- The run id is kept across `consume()` so it stays monotonic. An internal constructor takes a `DataStore`
  for tests; the `@Inject` one takes the app context.
- `DropboxSyncOutcomeStoreTest`: null when empty, message/reveal round-trip (with/without elevation),
  A-then-B last-write-wins, consume clears. Nothing reads/writes the store yet (#4/#5 of the parent).

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #207 — Add WorkManager + Hilt-Work infrastructure
Completed: 2026-09-19

### What was done

- Added `work-runtime-ktx` 2.10.0, `androidx.hilt:hilt-work` and `androidx.hilt:hilt-compiler` (KSP, alongside
  the Dagger one; versioned off `hiltNavigationCompose`) plus `work-testing` for tests.
- `VelometricsApplication` implements `Configuration.Provider` with an injected `HiltWorkerFactory`; the manifest
  removes the default `WorkManagerInitializer` under `androidx.startup.InitializationProvider`.
- `SmokeTestWorker` (`@HiltWorker`, logs + `Result.success()`) is left as a template; `SmokeTestWorkerTest`
  enqueues it via `WorkManagerTestInitHelper` and asserts SUCCEEDED.
- Not verified on device (startup with the real Configuration.Provider path only covered by the build/merge).

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #206 — Atomic per-file import: FitImportService writes in one Room transaction
Completed: 2026-09-19

### What was done

- `FitImportService` now injects `VelometricsDatabase` and wraps `insertSession` through `updateTag`
  (best efforts, intervals + matching, sprint update, tag) in one `withTransaction`; parse/hash/filter/
  metrics stay outside. A kill mid-import leaves nothing or a full row -- never a partial one that dedup
  would treat as imported. Public signature and callers unchanged.
- New `FitImportServiceTransactionTest` (Robolectric + real Room, FIT built with the Garmin encoder):
  successful import commits session + best efforts + tag; a forced `updateTag` failure leaves zero rows
  (`existsByFileName` false) and a retry then imports cleanly.

`testDebugUnitTest` passes.

