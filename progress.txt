## Issue #166 — Simplify: larger UI extraction (map drawer, chart drag gesture)
Completed: 2026-08-30

### What was done

Sixth `Simplify:` mechanical-cleanup issue in the run (#161-#165 landed the five sessions before
this one), same shape again: two named extractions, fully scoped in the issue body with exact
target signatures/behaviors and an explicit non-goal called out (don't merge `MonthlyLineChart`/
`PowerCurveChart` beyond the one shared gesture loop — they genuinely differ in axis scale,
selection-state ownership, and a header row). Read all four named screen files before editing, no
exploration subagent needed.

**`ui/components/TrackMapWithDrawer.kt`** (new) — shared "fit track to viewport in a drawer" map
composable extracted from `SessionDetailScreen.SessionDetailMap` and
`RepeatedIntervalDetailScreen.RepeatedIntervalDetailMap` (both byte-identical in
empty-state/BoxWithConstraints/re-center-on-drag/initial-camera-fit code, differing only in the
GPS-points source, the map's track-layer id, and whether a legend overlay renders). Takes
`points: List<LatLng>` (not a raw `gpsTrack: String?` — `RepeatedIntervalDetailScreen`'s points
were already pre-parsed upstream, so the common denominator is the parsed list; `SessionDetailMap`
keeps its own `remember(gpsTrack) { GpsTrackParser.parse(gpsTrack) }` above the call), `trackId`,
`trackColor` (defaults to the `"#2196F3"` both originals hardcoded), an `onMapReady: (MapLibreMap,
Style) -> Unit = { _, _ -> }` escape hatch, and an `overlayContent: @Composable
BoxWithConstraintsScope.() -> Unit = {}` slot. The `onMapReady` hook exists because
`SessionDetailScreen`'s interval-overlay *map layer* (`MapIntervalRenderer`, rendered onto the
MapLibre `Style` itself, not a Compose overlay) needs the `Style` instance on every intervals
change via its own `LaunchedEffect(mapStyleRef.value, intervals)` — that stays in
`SessionDetailScreen`, hooked in through `onMapReady = { _, style -> mapStyleRef.value = style }`,
untouched otherwise. `overlayContent` carries the session screen's Compose-UI `IntervalMapLegend`
(passed as `{ if (intervals.isNotEmpty()) IntervalMapLegend(...) }`); `RepeatedIntervalDetailScreen`
now calls `TrackMapWithDrawer` directly inline with no wrapper function at all, since it has
neither a legend nor a map-style hook left once the extraction is done.

**`ui/components/ChartGestures.kt`** (new) — `Modifier.dragToSelectGesture(vararg keys: Any?,
onSelect: (Float) -> Unit)`, wrapping the byte-identical `awaitEachGesture` drag-select loop
(select on down, re-select continuously while pressed, stop on lift) shared by
`HomeScreen.MonthlyLineChart` and `AllTimeStatsScreen.PowerCurveChart`. Only this loop moved —
`onSelect` is the one seam needed, since the two originals differed there: `MonthlyLineChart`
computed an index from x and called an `onMonthSelected` callback (externally-owned selection
state), `PowerCurveChart` computed the index and assigned directly to a local `selectedIndex` var
(internally-owned). `vararg keys` (spread into `Modifier.pointerInput(*keys)`) preserves each
call site's own invalidation keys unchanged — `dragToSelectGesture(monthlyData, chartWidthPx)` at
the Home screen call site, `dragToSelectGesture(points)` at the All-Time Stats one. Per the issue's
explicit instruction, nothing else in either chart (linear vs. log x-axis mapping, selection-state
ownership, `PowerCurveChart`'s header row) was touched.

### Tests
No test changes, per the issue's own scope — pure structural de-duplication of two Compose-only
UI shapes, each verified equivalent to its originals by construction (identical
BoxWithConstraints/Canvas/gesture code, only the call-site data and one callback seam vary), same
precedent as #161-#165 for this class of refactor. `compileDebugKotlin` and full
`testDebugUnitTest`: both pass clean (no Compose tests exist for any of these four screens, so this
only confirms no compile/unit-test regressions elsewhere). No on-device manual verification this
session (issue's suggested check — map camera fit/re-centering and the interval legend on Session
Detail and Repeated Interval Detail, drag-to-select on the Home and All-Time Stats line charts —
has no automated equivalent in this environment; verified each extraction line-for-line against
its pre-refactor originals at review time instead, consistent with #161-#165's precedent).

---

## Issue #165 — Simplify: UI structural duplication (charts, dialogs, lists)
Completed: 2026-08-30

### What was done

Fifth `Simplify:` mechanical-cleanup issue in the run (#161/#162/#163/#164 landed the four
sessions before this one), same shape again but the largest yet: five separate extractions spread
across nine files, fully scoped in the issue body with exact target signatures/behaviors and every
deliberate non-normalization named. Read every named file before editing, no exploration subagent
needed.

**`ui/components/TickedBarChart.kt`** (new) — shared bar+tick-mark `Canvas` extracted from
`PowerZoneChart`, `HeartRateZoneChart`, `SpeedHistogramChart` (all three were byte-identical in
layout/canvas code, differing only in zone/bin data, colors, and two label-formatting details).
`TickedBarEntry(label, percentage, color, percentageLabel: String?, tickPercentage: Float?)` — the
`percentageLabel` field (not a `showLabel: Boolean`) doubles as the one real behavioral difference
the issue called out (`PowerZoneChart`/`HeartRateZoneChart` pass `null` at 0% to hide the label,
`SpeedHistogramChart` always passes a formatted string) *and* preserves a second, unstated
difference the issue didn't call out but the original code had: `toInt()` (truncating) vs.
`roundToInt()` (rounding) percentage formatting between the two chart families — each caller
formats its own label text, so both differences survive without needing two separate flags. All
three chart files now just build a `List<TickedBarEntry>` from their own zone/bin maps and call
`TickedBarChart(title, entries, modifier)`; public function signatures
(`PowerZoneChart`/`HeartRateZoneChart`/`SpeedHistogramChart`/`SpeedHistogramChartAvg`) unchanged.

**`ui/components/EditableTopBarTitle.kt`** (new) — `EditableTopBarTitleState` (isEditing/editName/
focusRequester) + `rememberEditableTopBarTitleState(name)` (re-syncs `editName` from the backing
name whenever it changes and the user isn't mid-edit; requests focus on entering edit mode) +
`EditableTopBarTitle` (title-slot composable, text field or plain `Text`) +
`RowScope.EditableTopBarActions` (check/edit icon buttons, `extraActions: @Composable RowScope.()
-> Unit = {}` slot after the edit button). `EditableTopBarActions` needed a `RowScope` receiver,
not just a plain `@Composable` — first `compileDebugKotlin` attempt failed ("No value passed for
parameter 'p1'") because `extraActions()` (typed `RowScope.() -> Unit`) has no receiver to invoke
against without one. `RepeatedRouteDetailScreen` passes its "Export GPX" `IconButton` (only when
`representativeTrack` is non-empty) as `extraActions`; `RepeatedIntervalDetailScreen` passes
nothing, matching the issue's explicit "Interval screen doesn't have it" call-out.

**`SettingsScreen.kt`** (colocated, per the issue) — private `NumberEditDialog(title, label,
currentValue, helperText, onDismiss, onConfirm: (Int) -> Unit)` replaces the FTP/Max-HR edit
dialogs (validates trimmed input parses to a positive int different from `currentValue` before
calling `onConfirm`, otherwise the dialog just stays open — same as before extraction); private
`ConfirmDialog(title, text, confirmLabel, onConfirm, onDismiss)` replaces the FTP-change/Max-HR-
change/recalculate confirmations. Dropbox-folder dialog left untouched (confirmed byte-identical
post-edit) — different shape (text vs. number validation), single use, as the issue specified.

**`ui/components/RefreshableList.kt`** (new, generic `<T>`) — replaces
`RepeatedRoutesContent`/`RepeatedIntervalsContent` in `RepeatedRoutesScreen.kt`: pull-to-refresh +
loading spinner + empty-state (title/subtitle) + `LazyColumn` shape, parameterized by `entries`,
`key`, `emptyTitle`/`emptySubtitle`, and an `itemContent` slot. Named the list parameter `entries`
rather than `items` deliberately — a parameter literally named `items` shadows
`androidx.compose.foundation.lazy.items` inside the same composable, which would break the
`items(entries, key = key) { ... }` call inside `LazyColumn`. Also added a private, colocated
`<T : Enum<T>> SortDropdownItems(dimensions, currentOrder, onSelect)` in `RepeatedRoutesScreen.kt`
itself (per the issue: "in the same file") replacing the two duplicated enum-sort `DropdownMenu`
bodies (route dimensions vs. interval dimensions) — generic over any enum whose ASC/DESC variants
follow the existing `_ASC`/`_DESC` naming convention the arrow-glyph logic already relied on.

**`ui/components/LoadingBox.kt`** (new) — `LoadingBox(modifier)` and `NotFoundBox(text, modifier,
style, color)`, both internally `Modifier.fillMaxSize().then(modifier)` so a call site's own
modifier (e.g. `Modifier.padding(padding)`) composes onto the *same* chain position the original
inline `Box(Modifier.fillMaxSize().padding(padding), ...)` used — verified this reproduces each of
the 4 screens' pre-existing modifier exactly, including the one deliberate inconsistency called out
in the issue: `RepeatedRouteDetailScreen`/`RepeatedIntervalDetailScreen`'s loading spinner omits
`.padding(padding)` (both now call bare `LoadingBox()`) while `AllTimeStatsScreen`/
`SessionDetailScreen`'s include it (`LoadingBox(modifier = Modifier.padding(padding))`) — left
as-is, not fixed, per the issue. `NotFoundBox`'s `style`/`color` default to `LocalTextStyle.current`/
`Color.Unspecified` (plain `Text()`'s own defaults) so the three literal "X not found" call sites
(Session/Route/Interval) are unchanged; `AllTimeStatsScreen`'s distinctly-styled "No rides recorded
yet" empty state (`titleMedium`/`onSurfaceVariant`, not a literal "not found" case but the same
Box+Text shape) passes those explicitly to preserve its different look.

### Tests
No test changes — pure structural/mechanical de-duplication across 5 extractions, each verified
equivalent to its originals by construction (identical Canvas/dialog/list/box code, only the
call-site data varies) rather than by new test coverage, matching #161-#164's precedent for this
class of refactor. `compileDebugKotlin`, `compileDebugUnitTestKotlin`, `assembleDebug`, full
`testDebugUnitTest`: all pass clean, zero regressions (no Compose tests exist for any of these
screens, so this only confirms no compile/unit-test regressions elsewhere, per the issue's own
acceptance criteria). No on-device manual verification this session (issue's suggested check —
charts render identically, rename flow works on both detail screens including GPX export, all 4
Settings dialogs behave identically, Routes/Intervals lists/refresh/empty-states/sort menus behave
identically — has no automated equivalent in this environment; each extraction's call-site
equivalence was verified line-for-line against the pre-refactor originals at review time instead,
consistent with #161-#164's precedent).

---

## Issue #164 — Simplify: domain logic consolidation
Completed: 2026-08-30

### What was done

Third `Simplify:` mechanical-cleanup issue in a row (#161/#162/#163 landed the three sessions
before this one), same shape again: fully scoped in the issue body with exact target
signatures/behaviors named, so went straight to reading every named file before editing, no
exploration subagent needed.

**`domain/service/RankedMetricEvaluator.kt`** (new, `object`) — extracted the ranking machinery
`RideMilestoneEvaluator` (#159) and `PowerCurveAchievementEvaluator` (#160) each duplicated
verbatim: `rankCandidate` (count-greater-than -> rank -> candidate-if-top-3), `startOfYear`, and
the ordinal-headline construction ("Xxx"/"2nd-Xxx"/"3rd-Xxx" + noun + "ever"/"this year"). The two
originals' headline vocab wasn't quite identical text (`RideMilestoneEvaluator` varies its base
word per metric — "longest"/"most"/"fastest"; `PowerCurveAchievementEvaluator` always uses "best")
but the *shape* was identical, so `Metric<T>` carries a per-metric `ordinalWord` field — ride
milestones pass their own varying word, power-curve metrics all pass the literal `"best"` — and
`rank(metrics, yearStart, family): List<RideRevealCandidate>` is generic over the metric's value
type (`Double` for ride milestones, `Int` for power-curve watts) so both evaluators' `countGreater`
lambda signatures still typecheck without a cast. Both evaluators now just build their own
metric-and-query list and call `RankedMetricEvaluator.rank(...)`; verified byte-identical headline
output for both by construction (same interpolated template, same word-selection logic, just
lifted to a shared function) rather than by re-deriving it from scratch.

**`SessionMetricsCalculator.compute()`** — the 6 separate `if (hasPower) ... else null` guards for
`powerZoneDistribution`/`averagePower`/`normalizedPower`/`fatBurnedGrams`/`carbsBurnedGrams`/
`fatEfficiencyHistogram`/`fatEfficiencyScore` (7 fields, `effectivePowers` itself an 8th
power-gated local feeding 4 of them) collapsed into one `computePowerMetrics(datapoints, ftp):
PowerMetrics` call behind a single `if (hasPower)`, returning a private `PowerMetrics` data class
with the same nullability each field's compute function already had (`averagePower`/
`normalizedPower`/`fatEfficiencyScore` stay `Int?`, since e.g. `computeAveragePower` can itself
return null on an empty power list; the rest are non-null). `powerQualityPercent`'s
`hasPower && datapoints.isNotEmpty()` and `cardiacDrift`'s `hasPower && avgHeartRate != null`
guards left untouched per the issue's explicit call-out — genuinely different conditions, folding
them in would change behavior. Also renamed `computePowerZones`'s shadowed `val ftp =
ftp.toDouble()` local to `ftpDouble` (pure rename, the shadowing warning is gone but the logic is
unchanged).

**`FitImportService.filterGpsQuality`** — replaced the 5 parallel `var discardSpeed`/
`discardPower`/`discardZero`/`discardLeap`/`discardImplied` counters with one
`mutableMapOf("zero" to 0, "speed" to 0, "power" to 0, "leap" to 0, "implied" to 0)`, incremented
via `discardCounts["key"] = discardCounts.getValue("key") + 1` at each of the 5 discard sites. The
final `Log.d` line's format string is untouched byte-for-byte (same `discardZero=$x,
discardSpeed=$x, ...` label text and ordering), just reading `discardCounts.getValue("zero")` etc.
instead of the old individual vars.

### Tests
No test modifications, per the issue's own acceptance criteria — `RideMilestoneEvaluatorTest`,
`PowerCurveAchievementEvaluatorTest`, and `SessionMetricsCalculatorTest` all stayed green as-is
against the refactored code, confirming byte-identical behavior. `./gradlew testDebugUnitTest`
(targeted run against the three named test classes, then the full suite): both pass clean, no
regressions. `git diff --stat -- '*Test.kt'` confirms zero test files touched. No automated
coverage exists for `FitImportService` (pre-existing, not introduced here) so the issue's suggested
manual check (import a real FIT file, diff the discard-count debug log line before/after) wasn't
run in this environment — covered instead by the code-level argument above: the log line's format
string and variable-read sequence are unchanged, only the storage (5 vars -> 1 map) changed.

---

## Issue #163 — Simplify: trivial UI mechanical fixes
Completed: 2026-08-29

### What was done

Three small, purely mechanical clean-ups, fully scoped in the issue body with exact target
signatures/behaviors named — no exploration subagent needed, went straight to reading every named
file before editing.

**Manual ARGB construction → `Color.copy(alpha = ...).toArgb()`** — replaced all 5 sites (matching
the pattern already used correctly in `ScatterPlotChart.kt`):
- `CardiacDriftChart.kt`: the minute-label paint's fixed 0.6 alpha.
- `HomeScreen.kt` (`MonthlyLineChart`): the y-axis label paint's fixed 0.5 alpha, and the per-point
  month-label paint whose alpha toggled between full (255) and half (0.5) by selection state —
  collapsed the `val textAlpha = if (isSelected) 255 else (0.5f*255).toInt()` local into an inline
  `onSurface.copy(alpha = if (isSelected) 1f else 0.5f)`.
- `AllTimeStatsScreen.kt` (`PowerCurveChart`): the same two shapes (fixed 0.5 alpha tick labels;
  full/half alpha per-point labels by selection).
All 5 needed a new `androidx.compose.ui.graphics.toArgb` import (each file already imported
`Color` for other uses, but not `toArgb`).

**`FormatUtils.formatWithThousandsSeparator(value: Int): String`** — added the period-grouped
helper (`"1.234"`, not `"1,234"` — a different, pre-existing separator style used elsewhere by
`formatDistanceRounded`/etc., which the issue explicitly said not to switch to). Replaced its two
hand-rolled duplicates: `SessionEnergy.withThousandsSeparator` (private companion fun, deleted,
`formatTotalKcal()` now calls `FormatUtils.formatWithThousandsSeparator` directly) and
`HomeScreen.kt`'s inline `km.toString().reversed().chunked(3).joinToString(".").reversed()` in the
monthly "Total km" stat cell.

**Deleted `YearStatCell` (`AllTimeStatsScreen.kt`) and `MetricStatCell`
(`RepeatedRouteDetailScreen.kt`)** — both byte-for-byte duplicates of `ui/components/MetricCell.kt`
(same `labelSmall`/onSurfaceVariant label + `bodyMedium` value styling). `MetricCell` itself takes
no `modifier` parameter (callers wrap it in a weighted `Column` instead, per its existing use in
`SessionDetailScreen`/`RepeatedIntervalDetailScreen`), so every repointed call site changed from
`YearStatCell(label, value, Modifier.weight(1f))` / `MetricStatCell(label=, value=,
modifier=Modifier.weight(1f))` to `Column(modifier = Modifier.weight(1f)) { MetricCell(label =,
value =) }` — 4 call sites in `AllTimeStatsScreen.kt`'s `YearBreakdownSection`, 7 in
`RepeatedRouteDetailScreen.kt`'s stats grid. `MonthStatCell` (`HomeScreen.kt`) deliberately left
untouched per the issue's explicit warning — it renders values in `headlineSmall`, not
`MetricCell`'s `bodyMedium`, so merging it would visibly shrink Home-screen text.

### Tests
New `formatWithThousandsSeparator` cases in `FormatUtilsTest` (below-1000 passthrough, single
separator, multiple separators). No other new tests — the ARGB and duplicate-cell changes are pure
mechanical equivalents (verified alpha/color values match the originals exactly) with no new
branching logic. `./gradlew testDebugUnitTest` and `assembleDebug`: both pass clean, no
regressions. `grep`-verified zero remaining `android.graphics.Color.argb(` construction from a
Compose `Color` anywhere under `ui/`, and zero remaining `YearStatCell`/`MetricStatCell`
references. No on-device manual check this session (issue's suggested check — Home, All-Time
Stats, Session Detail's cardiac drift chart, and Repeated Route Detail should look pixel-identical
— has no automated equivalent in this environment; each replacement was checked against its
original for exact color/style equivalence at review time instead).

---

## Issue #162 — Simplify: JSON parsing & entity-mapping boilerplate
Completed: 2026-08-29

### What was done

Pure de-duplication issue, fully scoped in the issue body (three named extractions, exact target
signatures given) — same shape as #161's util-extraction the session before this one, so went
straight to reading every cited call site before writing any code, no exploration subagent needed.

**`util/JsonSafeParser.kt`** (new, `object`)
- `inline fun <reified T> parseOrDefault(json, tag, errorMessage, default): T` — the "try
  gson.fromJson, log + fall back to a default on failure" shape duplicated across 6 call sites in
  `RouteClusteringService`, `IntervalClusteringService`, `IntervalMatcher`, and both repeated-*
  repository impls. Added an internal `result ?: default` null-coalesce beyond a literal lift of
  the original bodies: two of the six originals (`IntervalClusteringService`/`IntervalMatcher`'s
  `parseGpsTrack`) explicitly guarded against `gson.fromJson` returning `null` on a successful-but-
  degenerate parse (e.g. a literal `"null"` payload) with their own `?: emptyList()`; folding that
  into the shared helper preserves those two call sites' behavior exactly while also hardening the
  other four against the same latent edge case they hadn't guarded against. `RepeatedIntervalRepositoryImpl.parseEdgeRefs`
  was deliberately left alone (not among the 6) — it parses to one type then transforms the result
  into `List<Pair<Long,Long>>` inside the same try block, a different shape from "parse to T,
  return T," matching how the issue already carved out `util/GpsTrackParser.kt` for an analogous
  reason (a different concern bolted onto the parse).
- `gson` is a `@PublishedApi internal val`, not `private` — a public inline function can't close
  over a private property (`compileDebugKotlin` caught this immediately: "Public-API inline
  function cannot access non-public-API property").

**`util/EntityMappers.kt`**
- Added `String.parseJson<T>()`, `String?.parseJsonOrNull<T>()`, `T.toJsonString()` — same
  reified-inline-plus-`@PublishedApi internal gson`-instance shape as `JsonSafeParser`, but with no
  tag/default/logging (these fields have no persisted-JSON-failure fallback path today; `parseJson`
  on a malformed non-nullable field still throws, matching the pre-existing behavior exactly). Used
  to replace `CyclingSessionEntity.toDomain()`/`toEntity()`'s 6 manual `gson.fromJson`/`gson.toJson`
  pairs (power-zone/speed/fat-efficiency/sprint/HR-zone histograms plus cardiac-drift buckets) and
  removed the now-unused `mapType`/`doubleMapType` locals.

**`data/repository/RepositoryUtils.kt`** (new, top-level `suspend fun`)
- `resolveCreatedAt(id, lookupExisting): Long` — the identical "existing row keeps its `createdAt`,
  new row gets `now`" 5-line block from `RepeatedRouteRepositoryImpl.saveRoute` and
  `RepeatedIntervalRepositoryImpl.saveRepeatedInterval`, both now a one-line call passing their own
  DAO lookup as the lambda.

### Tests
No new tests — pure mechanical de-duplication, zero behavior change at 4 of 6 `JsonSafeParser`
call sites and a documented, deliberate hardening (not a behavior change in practice) at the other
2; `resolveCreatedAt`/`EntityMappers` helpers are likewise line-for-line equivalent to what they
replaced. `./gradlew testDebugUnitTest`: all pass with zero test modifications, per the issue's own
acceptance criteria. No on-device manual check this session (the issue's suggested manual check —
import a FIT file, rename a repeated route/interval, reimport/resave, confirm `createdAt` survival
and identical JSON round-trips — has no automated equivalent in this environment; the code-level
equivalence argument above covers the same ground for this class of refactor, consistent with
#161's precedent for mechanical extractions the session before this one).

---

## Issue #161 — Simplify: domain graph/spatial/math utilities
Completed: 2026-08-29

### What was done

Pure de-duplication issue, fully scoped in the issue body itself (three named extractions, exact
target signatures given) — planned via Explore + Plan subagents against the five source files, then
verified every cited line range by direct read before writing the plan, since the plan was handed
straight to implementation with no further exploration pass.

**`util/GraphUtils.kt`** (new, `object`)
- `connectedComponents(n, adjacency: Array<out MutableList<Int>>): List<List<Int>>` — the
  identical BFS block from `RouteClusteringService.runClustering()` and
  `IntervalClusteringService.runClustering()`, lifted verbatim (componentId/BFS/nextComponent),
  returning components in discovery order. `n == 0` returns `emptyList()`, matching both originals.

**`util/SpatialPointGrid.kt`** (new, `class`, replacing `RouteClusteringService.SpatialGrid` and
`IntervalSimilarity.SpatialGrid`)
- `SpatialPointGrid(points, cellRadiusM)` with `hasPointWithin(lat, lon): Boolean` — radius is now
  a constructor param instead of a closed-over constant (`RouteClusteringService`'s was a `private
  inner class` reading `MATCH_THRESHOLD_M`; `IntervalSimilarity`'s was a top-level nested `class`
  reading `INTERVAL_POINT_MATCH_RADIUS_M`), so both call sites now pass their own constant
  explicitly. Adopted `IntervalSimilarity`'s empty-list guard on the avgLat calc (the other
  original divided by `points.size` unguarded). Both call sites' hardcoded `111_320.0` replaced
  with `GeoUtils.METERS_PER_DEG_LAT`; `IntervalSimilarity.bboxesWithinMatchRange` had two more
  hardcoded occurrences of the same literal (a separate function, not part of the grid class),
  fixed too since the acceptance criteria named the file, not just the class.

**`util/MathUtils.kt`** (new, top-level extension)
- `fun List<Double>.median(): Double?`, null only on a genuinely empty list. `SessionComparator`'s
  private `median()` becomes a one-line wrapper keeping its own "needs ≥2 samples" business rule:
  `if (values.size < 2) null else values.median()`. `SessionMetricsCalculator`'s inline duplicate
  (in `computeEffectivePowers`, substituting a plausible wattage for zero-power streaks) becomes
  `prevPowers.median() ?: 0.0` — verified byte-identical behavior for empty (→0.0 fallback, same as
  before), single-element ( → that element, same as before's `sorted[mid]` at `mid=0`), and 2+
  element inputs across both call sites.

**Related but explicitly not touched**: `MapMatcher.findConnectingPath` is a bounded
shortest-path BFS with path reconstruction between two specific nodes — a different shape/purpose
from the connected-components labeling BFS extracted here, flagged during exploration and left
alone.

### Tests
New `GraphUtilsTest` (7 cases, no existing coverage previously existed for this logic): empty
graph, single isolated node, fully-connected triangle, disconnected nodes, mixed-size clusters,
duplicate-edges/self-loops don't affect grouping, every node appears exactly once across returned
components. `./gradlew testDebugUnitTest`: all pass with zero modifications to
`IntervalClusteringServiceTest`, `SessionComparatorTest`, `SessionMetricsCalculatorTest` — none of
them reference the internals that moved, only public service APIs. `grep`-verified zero remaining
`111_320.0` literals, `SpatialGrid` classes, or inline `componentId = IntArray` blocks in
`app/src/main` outside the new util files. No on-device smoke test this session (no functional
behavior changed — pure extraction verified equivalent line-by-line against the originals, not a
substitute for the issue's own suggested manual check, but the unit-level equivalence proof covers
the same ground for this kind of mechanical refactor).

---

## Issue #160 — Ride Reveal: power-curve best-effort achievements
Completed: 2026-08-29

### What was done

Only `ready-for-agent` issue in the queue — the other half of the Ride Reveal Tier 1 achievement
pair alongside #159 (ride-level milestones), which landed earlier this session and gave this one's
milestone-beats-power-curve tie-break something genuine to verify against instead of a contrived
resolver-only test. Mirrors #159's rank-query shape almost exactly ("count of best efforts with a
strictly greater value, plus 1", scoped by an optional `since` bound), applied to
`BestEffortCalculator`'s 5s/1min/5min/20min power-curve fields instead of raw session stats.

One structural difference from #159: a ride-level metric (distance, elevation, speed) lives right
on the `CyclingSession` object `RideMilestoneEvaluator.candidates()` already receives, but power-curve
bests live in a separate `session_best_efforts` row that `RideRevealEvaluator` wasn't fetching.
Added `BestEffortRepository.getForSession(sessionId)` (a single-row DAO lookup by `sessionId`, mapped
to the same `BestEffortValues` domain type the calculator already produces) so
`PowerCurveAchievementEvaluator.candidates(sessionId, sessionStart)` can look up the ride's own bests
before ranking them.

**`data/local/dao/SessionBestEffortDao.kt` (+ `BestEffortRepository`/Impl, `FakeBestEffortRepository`)**
- `getBySessionId(sessionId): SessionBestEffortEntity?` and four `countBestEffortsWithGreaterPower{5s,
  1m,5m,20m}(power, sinceEpochMs: Long?)` queries, each `SELECT COUNT(*) FROM session_best_efforts sbe
  JOIN cycling_sessions cs ON cs.id = sbe.sessionId WHERE sbe.powerX > :power AND (:sinceEpochMs IS
  NULL OR cs.sessionStart >= :sinceEpochMs)` — same join-for-the-date-scope shape #159 used, needed
  here because `session_best_efforts` itself has no date column. A NULL power value (no power meter
  that ride, or the ride was shorter than the duration) is excluded automatically by SQLite's
  NULL-comparison semantics, same as #159's NULL-elevation handling. No new index (same personal-scale
  non-issue precedent). `FakeBestEffortRepository` computes both `getForSession` and the four counts
  directly off its existing `records: MutableList<BestEffortRecord>` — that type already carries
  `sessionId`, `sessionStart`, and every power field, so no new fake-side storage was needed.

**`domain/service/PowerCurveAchievementEvaluator.kt`** (new, `@Singleton`, injects `BestEffortRepository`)
- `candidates(sessionId, sessionStart): List<RideRevealCandidate>`: fetches the ride's own best-effort
  row via `getForSession`; returns `emptyList()` outright if there isn't one (no power meter that
  ride). Builds a metric per non-null duration (5s/1m/5m/20m — up to 4, fewer if some durations are
  null e.g. a shorter ride missing 20-min), each carrying its own count-greater-than query and a
  headline label ("5-second power" etc). For each metric, queries both scopes and registers a
  `POWER_CURVE_BEST_EFFORT`-family candidate for every scope where `count + 1 <= 3` — up to 8
  candidates per ride. Headline template ("Your {best|2nd-best|3rd-best} {duration} power {ever|this
  year}!") mirrors #159's ordinal-superlative approach.

**`domain/service/RideRevealEvaluator.kt`**
- Constructor gained `PowerCurveAchievementEvaluator`; `evaluate()` now passes
  `milestoneEvaluator.candidates(session) + powerCurveEvaluator.candidates(session.id,
  session.sessionStart) + fallbackCandidate(session)` to the resolver. Class doc comment updated to
  name both Tier 1 sources as landed rather than one pending.

### Tests
New `PowerCurveAchievementEvaluatorTest` (7 cases, via `FakeBestEffortRepository`): a first-ever ride
ranks 1st all-time on all 4 durations; a ride beaten by 3 others on a duration doesn't qualify on it;
a ride beaten by only 2 others still qualifies at rank 3 (no-sample-size-floor case, same as #159);
this-year scope excludes a bigger effort from a different calendar year; a ride with no best-effort
row at all (no power meter) registers zero candidates; a null power value for one duration (ride
shorter than 20 minutes) registers no candidate for that duration specifically, others unaffected;
headline text for a rank-1 all-time 5-second power PR. Extended `RideRevealEvaluatorTest` (+2 cases,
constructor now takes `PowerCurveAchievementEvaluator` too): a ride that's simultaneously the
longest-ever *and* a rank-1 20-minute power PR shows the milestone headline, not the power-curve one
— the acceptance criteria's required tie-break check, now running against two genuine qualifying
achievements instead of a contrived case; a ride with no ride-level milestone (beaten on every
tracked metric) but a genuine rank-1 20-minute power PR shows the power-curve headline instead of
falling back. `compileDebugKotlin`, `compileDebugUnitTestKotlin`, `assembleDebug`, full
`testDebugUnitTest --rerun-tasks`: all pass (260 tests, up from 251 — 9 new), no regressions. No
on-device verification this session (pure rank-query/candidate logic fully covered by the new unit
tests against a fake repository; no new UI surface — the sheet itself is unchanged from #158).

---

## Issue #159 — Ride Reveal: ride-level milestone achievements
Completed: 2026-08-29

### What was done

Second Ride Reveal slice, picked over #160 (power-curve best-effort achievements) even though
both were unblocked by #158 landing earlier this session — #160's own acceptance criteria needs a
real ride-level milestone candidate to exist to verify the milestone-beats-power-curve tie-break,
so building this one first gives that check something genuine to run against instead of a
contrived resolver-only test.

Rank query shape follows the design doc's own suggestion almost exactly: "rank = count of rides
with a strictly greater value, plus 1", scoped by an optional `since` bound (null = all-time, or
the ride's own calendar-year start for "this year"). Deliberately used the ride's own
`sessionStart` year (via `ZoneId.systemDefault()`), not wall-clock "now" — the two coincide for a
ride that just synced (the only time this runs today), but stays correct if a batch is ever
evaluated after the fact instead of live. Confirmed the acceptance criteria's "no minimum
sample-size threshold" assumption is genuinely load-bearing with a dedicated test: a ride with
only 3-4 total rides ever, ranking last of them, still registers as a "3rd place" milestone.

**`data/local/dao/CyclingSessionDao.kt` (+ `CyclingSessionRepository`/Impl, `FakeCyclingSessionRepository`)**
- Three new `countSessionsWithGreater{Distance,ElevationGain,AverageSpeed}(value, sinceEpochMs:
  Long?)` queries, each `SELECT COUNT(*) ... WHERE metric > :value AND (:sinceEpochMs IS NULL OR
  sessionStart >= :sinceEpochMs)`. No new index (matches #156/#158 precedent — a non-issue at this
  app's personal ride-count scale). A NULL `elevationGainM` column, or a `netDurationSec <= 0` row,
  is excluded from the "greater" count automatically by SQLite's NULL-comparison semantics for the
  first two, and by an explicit `netDurationSec > 0` guard for the average-speed query (computed
  in-SQL as `distanceKm / netDurationSec * 3600`, not a stored column) — no separate null-handling
  branch needed in the DAO. Repository interface takes `since: Instant?`, converting to epoch
  millis only at the Impl boundary, matching `getMaxSessionStart`'s existing `Instant`/`Long` split.
  `FakeCyclingSessionRepository` mirrors the same semantics with plain Kotlin filtering.

**`domain/service/RideMilestoneEvaluator.kt`** (new, `@Singleton`, injects `CyclingSessionRepository`)
- `candidates(session): List<RideRevealCandidate>`: builds a small in-memory list of the 2-3
  trackable metrics for this ride (distance always; elevation only if `elevationGainM != null`;
  average speed only if `netDurationSec > 0`), each carrying its own count-greater-than query, a
  headline vocabulary (superlative + noun, e.g. "longest"/"ride"), and its raw value. For each
  metric, queries both scopes (all-time and this-year) and registers a `RIDE_MILESTONE`-family
  candidate for every scope where `count + 1 <= 3` — up to 6 candidates per ride, all handed to
  `RideRevealEvaluator` for `RideRevealResolver` to pick among. Headline template
  ("Your {ordinal-superlative} {noun} {ever|this year}!") reuses the same superlative word for rank
  1 and prefixes "2nd-"/"3rd-" for ranks 2/3 ("Your longest ride ever!" ->
  "Your 2nd-longest ride ever!"), which reads naturally for all three metric nouns without
  per-metric-per-rank copy.

**`domain/service/RideRevealEvaluator.kt`**
- Constructor gained `RideMilestoneEvaluator`; `evaluate()` now passes
  `milestoneEvaluator.candidates(session) + fallbackCandidate(session)` to the resolver instead of
  just the fallback alone. Class doc comment updated to stop saying "today only the fallback is
  registered" and name both this and the still-pending #160 source.

### Tests
New `RideMilestoneEvaluatorTest` (7 cases, via `FakeCyclingSessionRepository`): a first-ever ride
ranks 1st all-time on all 3 metrics; a ride beaten by 3 others on a metric doesn't qualify on it
(rank 4, correctly excluded); a ride beaten by only 2 others still qualifies at rank 3 (the
no-sample-size-floor case); this-year scope excludes a bigger ride from a different calendar year
while all-time correctly counts it; a null `elevationGainM` registers no elevation candidate; a
zero-duration ride registers no average-speed candidate; headline text for a rank-1 all-time
distance PR. Extended `RideRevealEvaluatorTest` (+2 cases, `RideRevealEvaluator` constructor now
takes the new evaluator too): a genuine longest-ever ride shows the milestone headline instead of
the fallback; a ride beaten by 3 pre-existing rides on every tracked metric still falls back to
"Nice ride!" (regression guard for #158's guaranteed-fallback behavior). `compileDebugKotlin`,
`compileDebugUnitTestKotlin`, `assembleDebug`, full `testDebugUnitTest --rerun-tasks`: all pass
(251 tests, up from 242 — 9 new), no regressions. No on-device verification this session (pure
rank-query/candidate logic fully covered by the new unit tests against a fake repository; no new
UI surface — the sheet itself and its rendering were already covered by #158).

---

## Issue #158 — Ride Reveal: trigger + guaranteed fallback
Completed: 2026-08-29

### What was done

First slice of the Ride Reveal feature (design doc: `docs/ideas/ride-reveal-hero.md`, not yet
committed before this session). Picked over #159/#160 since both are explicitly blocked on this
one (they plug into the resolver/trigger this issue builds). Read the design doc's trigger-condition
section closely since it's the crux of the issue and has a subtle backfill trap: "identify the
single most-recently-imported file's ride" turns out to mean the *chronologically* newest ride
actually imported in the batch (by parsed FIT start-time), not the last file processed by name/
order — the doc's own clarifying sentence ("not filename or Dropbox processing order") only makes
sense under that reading, since `DropboxSyncService` sorts entries by filename before importing.
The backfill guard falls out for free from comparing against a *pre-sync baseline*: captured once
per batch via `getMaxSessionStart()` *before* any file in that batch is imported; a null baseline
(empty DB) means "never reveal," so a first-time Dropbox connect or fresh-install backfill of
hundreds of historical rides can't trigger it no matter which file in that batch is chronologically
last, since there's no pre-existing max to be "later than."

**`domain/repository/CyclingSessionRepository.kt` (+ Impl, DAO, `FakeCyclingSessionRepository`)**
- New `getMaxSessionStart(): Instant?` — a plain `SELECT MAX(sessionStart)`, null on an empty table.
  No new index; matches the design doc's explicit call that this is a non-issue at personal ride-
  count scale.

**`data/fitimport/ImportResult.kt`, `data/fitimport/FitImportService.kt`**
- `ImportResult.Success` gained a `sessionStart: Instant` field (the already-computed
  `session.sessionStart`, no extra DB round-trip) so callers can identify the batch's
  chronologically-newest imported ride without re-fetching every session.

**`domain/model/RideRevealCandidate.kt`** (new)
- `RideRevealScope` (ALL_TIME, THIS_YEAR), `RideRevealFamily` (RIDE_MILESTONE,
  POWER_CURVE_BEST_EFFORT, FALLBACK), and `RideRevealPriority(scope, rank, family):
  Comparable<RideRevealPriority>` implementing the design doc's exact tie-break ordering (scope,
  then rank, then family) via enum-ordinal comparison — declaring each enum's entries in
  best-first order makes the `compareTo` a one-liner per field, no explicit weight table needed.
  `RideRevealPriority.FALLBACK` is a companion constant (`THIS_YEAR`/`Int.MAX_VALUE`/`FALLBACK`)
  that's guaranteed to lose to any real 1st-3rd achievement in any scope, verified in
  `RideRevealResolverTest`. `RideRevealCandidate(headline, priority)` — deliberately just those two
  fields, matching the issue's "a label plus a priority key" description, so #159/#160 don't need
  any new shape to plug into.

**`domain/service/RideRevealResolver.kt`** (new)
- `resolve(candidates: List<RideRevealCandidate>): RideRevealCandidate` — a pure `reduce` picking
  the lowest-priority (best) candidate; throws on an empty list (callers always pass at least the
  fallback).

**`domain/service/RideRevealEvaluator.kt`** (new, `@Singleton`, injects `CyclingSessionRepository`)
- `captureBaseline()`: thin wrapper on `getMaxSessionStart()`, called once per batch *before* import
  starts.
- `evaluate(results, baseline)`: null baseline -> null (backfill guard). Otherwise takes
  `results.filterIsInstance<ImportResult.Success>().maxByOrNull { it.sessionStart }` (ignores
  `Error`/`AlreadyImported`/skipped-`SmallFile` entries) — if that ride's start isn't after
  `baseline`, returns null (routine sync, nothing newer than what already existed). Otherwise fetches
  the full `CyclingSession`, builds the single Tier 2 fallback candidate (static "Nice ride!"
  headline, `RideRevealPriority.FALLBACK`), resolves it (trivially, since it's the only candidate
  today), and returns `RideRevealContent(sessionId, headline, distanceKm, netDurationSec,
  elevationGainM)`. This is the one place #159/#160 will extend: add their candidate(s) to the same
  list before calling `RideRevealResolver.resolve`.

**`ui/screens/home/HomeViewModel.kt`**
- New `ImportUiState.RideReveal(content: RideRevealContent)`, shown instead of `Done`
  (`importFromUris`) or the Dropbox snackbar (`syncDropbox`) when `RideRevealEvaluator.evaluate`
  returns non-null.
- `importFromUris`: now accumulates every file's `ImportResult` into a list (previously only tracked
  `lastResult`, overwritten each iteration) so the evaluator can see the whole batch; captures the
  baseline before the loop starts. The existing early-return-on-unreadable-file quirk (aborts the
  rest of the batch, a pre-existing behavior out of scope here) is preserved via `break` instead of
  `return@withLock`, with identical downstream behavior since both paths fall through to the same
  final-state assignment now.
- `syncDropbox`: captures the baseline immediately before `dropboxSyncService.sync()`; on
  `DropboxSyncResult.Completed`, evaluates the reveal before falling back to the existing
  `buildSyncMessage`/snackbar path, so pull-to-refresh gets the same reveal behavior as manual
  import without duplicating the trigger logic.

**`ui/screens/home/HomeScreen.kt`**
- New `RideRevealSheet` (`ModalBottomSheet`, `skipPartiallyExpanded = true`): headline text, a
  distance/duration/elevation stat row (elevation cell omitted when null, matching how the rest of
  the app treats a missing `elevationGainM`), and a "View ride" button that dismisses the sheet and
  navigates via the screen's existing `onSessionClick`. Rendered alongside the pre-existing
  `SmallFileWarning` dialog block, gated on the same `currentState` snapshot.

**`docs/ideas/ride-reveal-hero.md`**: committed for the first time this session (existed untracked
in the working tree beforehand) since it's the design rationale this and the two follow-up issues
point to.

### Tests
New `RideRevealResolverTest` (6 cases: single-candidate trivial win, achievement beats fallback
regardless of list order, all-time beats this-year regardless of rank, lower rank wins within a
scope, ride-milestone beats power-curve within equal scope+rank, empty list throws) and
`RideRevealEvaluatorTest` (8 cases, via `FakeCyclingSessionRepository`: null baseline on an empty
DB, baseline = latest existing start, fires when later than baseline, doesn't fire when not later,
doesn't fire on a backfill into an empty DB even for the batch's newest ride, picks the
chronologically-newest batch member rather than the last one in the results list, ignores
non-`Success` results when picking the batch's newest ride, no successes never fires).
`compileDebugKotlin`, `compileDebugUnitTestKotlin`, `compileDebugAndroidTestKotlin`, `assembleDebug`,
full `testDebugUnitTest --rerun-tasks` (matching the known Gradle env-var/test-filter stale-cache
quirk from #135): all pass (242 tests, up from 228 — 14 new), no regressions. No on-device
verification this session (needs a real device with Dropbox configured and two ordered rides to
exercise the reveal end-to-end; the trigger/resolver logic is fully covered by the new unit tests,
and the sheet itself is a straightforward `ModalBottomSheet` with no novel rendering risk).

---

## Issue #157 — Session Detail: toggle for last-5/all-previous comparison triangles; flip elevation-gain polarity
Completed: 2026-08-28

### What was done

Full `/grill-me` design session before implementing (user's request was terse: "more climbing meters
are better," plus a toggle replacing the two-triangle display). Resolved: elevation-gain polarity
flips both `Elevation gain` and `Elev. gain / 100km` from `higherIsBetter = false` to `true`,
reversing #153's original "treat climbing like a fatigue cost, same as Calories" decision — accepted
explicitly as a real tradeoff (can no longer distinguish "climbed more because I pushed harder on my
usual route" from "climbed more because I chose a hillier route," achievement framing wins anyway);
no noise deadband added, stays an absolute `current > reference` comparison like every other metric;
toggle is a single global control for the whole screen (not per-metric), `FilterChip` row (matching
`MapViewScreen`'s POI-category chip pattern) with two static-label chips — "vs. last 5" / "vs. all" —
placed where `ComparisonLegend` used to sit, which is removed entirely; "last 5" keeps its existing
meaning (5 sessions immediately preceding the viewed one, unchanged `SessionComparator` query); chip
labels never show live counts, even with fewer than 5 (or 0) prior sessions; toggle selection is
local `remember` UI state, not persisted — always resets to "vs. last 5" on screen open; an
insufficient-data mode leaves its chip selectable and just renders metrics without a triangle, same
as the existing per-metric null-median handling. Confirmed via grep that no other screen
(`AllTimeStatsScreen`, `RepeatedRouteDetailViewModel`) encodes elevation-gain polarity or duplicates
this triangle logic, so the change is isolated to `MetricCell`/`SessionDetailScreen`.

**`ui/components/MetricCell.kt`**
- Collapsed the dual `medianLast5`/`medianAllPrevious` parameters (and the two triangles they each
  rendered) into a single `reference: Double?`, rendering at most one triangle. `getTriangle`'s
  direction/color logic is unchanged. `RepeatedIntervalDetailScreen`'s existing `MetricCell` calls
  never passed either median parameter, so they're unaffected by the signature change.

**`ui/screens/sessiondetail/SessionDetailScreen.kt`**
- New private `ComparisonMode` enum (`LAST_5`/`ALL_PREVIOUS`) and `ComparisonModeToggle` composable
  (two `FilterChip`s). `ComparisonLegend` deleted along with its now-unused `ArrowDropUp` import.
- `RideSummaryGrid` holds the toggle's `remember { mutableStateOf(ComparisonMode.LAST_5) }` state and
  a local `pooled(last5, allPrevious)` helper that all 10 comparable `MetricCell` calls use to pick
  which of `SessionComparison`'s paired median fields to pass as `reference`, based on the active
  mode. `Elevation gain`/`Elev. gain / 100km` calls flip to `higherIsBetter = true`; every other
  metric's polarity is unchanged. `SessionComparator` itself is untouched — both pools were already
  computed per #153, this is purely UI-side selection.

### Tests
No new automated tests (pure UI restructuring — parameter collapse, toggle-driven selection, two
polarity-flag flips — no new branching logic beyond what `getTriangle` already covers, matching #151's
precedent for Compose-only Modifier/parameter-plumbing changes). `compileDebugKotlin`,
`compileDebugUnitTestKotlin`, `assembleDebug`, full `testDebugUnitTest --rerun-tasks` (matching the
known Gradle env-var/test-filter stale-cache quirk from #135): all pass (228/228), no regressions. No
on-device verification this session (Compose-only visual change; the maintainer resolved every open
question — polarity, toggle scope, persistence, default, legend removal, empty-data handling, label
staticness — during the `/grill-me` session rather than leaving anything to visually confirm).
