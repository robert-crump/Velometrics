## Issue #198 — ChartAxis module: one tick/plot-rect/label implementation for the line and scatter charts
Completed: 2026-09-19

### What was done

- New `ui/components/ChartAxis.kt` (pure, unit-tested in `ChartAxisTest`): `integerAxisTicks`
  (generalised with `stepUnit` for 50 W / 5-ride grids) and `roundedAxisBounds` moved here from
  `ScatterPlotChart`, plus `evenAxisTicks`, `labelStride`, `PlotRect` (padding -> plot rect) and
  `LinearScale` (`map`/`invert`, replaces every per-chart `xFor`/`yFor`).
- New `ChartLabelPainter` is the only place that builds `android.graphics.Paint` for chart text.
- `ScatterPlotChart`, `CardiacDriftChart`, `TrainingLoadChart`, `PowerCurveChart` and Home's
  `MonthlyLineChart` are now thin adapters; the MonthlyLineChart drag hit-test uses `scale.invert`.
- Not verified on device (no screenshot comparison done); ride-count axis now steps 5 with at most
  5 intervals, so only counts above 25/month get a coarser step than before.

`testDebugUnitTest` passes.

## Issue #197 — Unify JSON serialization, duration formatting, and the thousands separator
Completed: 2026-09-19

### What was done

- New `util/Json` is the single owner of the app's only `Gson` instance (plus `toJson`/`parseOrDefault`);
  it replaces `JsonSafeParser` and the seven other `Gson()` forks (main + tests).
- `FormatUtils` is the one home for duration/thousands formatting: added `formatDurationMinSec`
  (moved from `MapOverlayUtils`) and `formatDecimal`; `formatDistanceRounded`,
  `formatElevationGainRounded`, `formatElevationGainPer100km` now use the period separator via
  `formatWithThousandsSeparator` (now negative-safe), so Home and All-time Stats agree ("1.234 km").
- `HomeAddressScreen`, `SettingsScreen`, `ScatterPlotChart` (`xTickFormat` -> `xTickDecimals`) and the
  InfoScreen kcal axis use `FormatUtils` instead of inline `"%.Nf".format`.
- `MapOverlayUtils` keeps only colour interpolation; `groupIntervals`, `avgDurationNormalizedSec`,
  `avgPower`, `isFlowSegment` moved to `domain/service/OverlayAggregation` (tests moved too).

`testDebugUnitTest` passes.

## Issue #196 — Delete dead route-composition modules; fold RideTagDumper into RideClassificationService; move UiState files out of domain
Completed: 2026-09-19

### What was done

- Deleted `RideEstimator`, `RewardComposer`, `RideDirection` (no production callers after #155) and
  their tests; the deferred/rejected routing-term notes went to `docs/ideas/route-reward-terms.md`.
- `RideTagDumper` is gone (plus the unused `DumpSessionTagsForReview` androidTest). `RideClassificationService`
  now has `reviewRows(ftp): List<TagReviewRow>`; `TagReviewRow.isStale` is the single home of the
  staleness rule and `reclassifyAll` uses it. CSV rendering moved to `ui/screens/settings/RideTagCsv`;
  the debug Settings row stays and does the file write.
- `AllTimeStatsUiState` (with `RecordEntry`, `YearStat`, `PowerSpeedPoint`) moved to
  `ui/screens/alltimestats`, `TrainingLoadUiState` (with `DailyTrainingLoadPoint`) to
  `ui/screens/trainingload`. `PowerCurvePoint` stays in `domain/model` (own file) since domain
  `BestEffort` uses it.
- Removed the discarded-result `computeComparison` loop from `SettingsViewModel.recalculateAllStats`
  and the now-unused `SessionComparator` constructor dependency.

`testDebugUnitTest` passes.

## Issue #199 — Map View: extract user-location renderer and feature query, remove MapLibre types from ViewModels, fix stale CONTEXT.md entry
Completed: 2026-09-18

### What was done

Three architecture-review fixes to `MapViewScreen`/`MapViewViewModel`, all from the same
`/improve-codebase-architecture` pass that produced #195.

**Extracted the ~110 raw-MapLibre lines out of `MapViewScreen`** — `MapUserLocationRenderer` (new,
`ui/components/`) owns the user-location dot/accuracy-circle/heading-arrow layers, taking a domain
`GeoPoint` instead of MapLibre's `LatLng`. `MapFeatureQuery` (new, same package) is a small adapter
over `queryRenderedFeatures` tap hit-testing, so the three interactive layers' names
(`MapPoiRenderer.POI_CLUSTER_LAYER`/`POI_LAYER`, `MapIntervalRenderer.GROUPED_LAYER` — the last made
public for this) live next to the renderers that create them instead of as string literals in the
screen's click listener. The accuracy-weighted location-smoothing math moved to a new pure
`util/LocationSmoothing.weightedAverage(List<LocationSample>): GeoPoint?`, unit-tested
(`LocationSmoothingTest`, 5 cases) — the screen now only owns the `mutableStateListOf` window and
calls the pure function. `MapViewScreen` no longer constructs `CircleLayer`/`SymbolLayer`/
`Expression` or references a layer name directly.

**Removed MapLibre types from both ViewModels' interfaces** — new map-library-free
`domain/model/GeoPoint`/`GeoBounds` (the latter with `contains(lat, lon)`) replace `LatLng`/
`LatLngBounds` throughout `MapViewViewModel` (`currentLocation`, `_viewportBounds`,
`updateViewportBounds`) and `RepeatedIntervalDetailViewModel` (`trackPoints`). Conversion to/from
MapLibre's types happens only at the UI edge, via new `GeoPoint.toLatLng()`/`LatLng.toGeoPoint()`/
`LatLngBounds.toGeoBounds()` extensions (`ui/components/GeoLibreExtensions.kt`) — `MapViewScreen`
and `RepeatedIntervalDetailScreen` call these at the boundary; `TrackMapWithDrawer` (a Composable)
was left taking `LatLng` since MapLibre types are the right currency for a map-rendering component,
just not for a ViewModel.

**Reinstated `PoiSelectionState` for Map View** — new `ui/screens/mapview/PoiSelectionState.kt`
(`activeChip`, `selected`, intents `selectChip`/`selectPoi`/`dismissPoi`) replaces
`MapViewViewModel`'s `_activePoiChip`/`_selectedPoi` pair, which the architecture review flagged as
a regression back to the "parallel `MutableStateFlow`s in lockstep" shape the glossary already
described (for the now-deleted Navigation screen). `_allPois` stayed a separate plain data flow —
it's the raw POI list, not a "what is the user looking at" concern, so folding it in would have
mixed two different kinds of state. `poiSelection: StateFlow<PoiSelectionState>` is exposed
directly (not via `.map{}.stateIn()`) so a synchronous read immediately after `selectPoiChip()`/
`selectPoiFromMap()` sees the new value without needing an active collector to pump the flow first
— tried the mapped/derived form initially and it broke `MapViewViewModelTest`'s no-`runCurrent()`
assertions, which was the tell that it would've been a real (if usually invisible) latency
regression, not just a test artifact. Selecting a chip intentionally leaves any popped-up POI
untouched (matches prior behavior exactly — confirmed with a new test). Updated `CONTEXT.md`'s
`PoiSelectionState` entry to describe the Map View shape and note the Navigation-screen history.

### Tests
New: `LocationSmoothingTest` (5 cases: empty window, single sample, equal-accuracy averaging,
accuracy-weighted pull, sub-1m clamping). `MapViewViewModelTest` gained one case (chip selection
leaves an open POI popup untouched) and had its existing POI-chip assertions updated for the
`poiSelection` state object. `compileDebugKotlin`, `compileDebugUnitTestKotlin`, `assembleDebug`,
full `testDebugUnitTest`: all pass. No on-device verification this session (no emulator/device
attached) — acceptance criterion "Map View behaves unchanged on device" is unverified beyond the
unit-test coverage of the extracted/refactored logic; the renderer/query extraction is a
line-for-line behavior-preserving move, and the POI-selection consolidation was deliberately kept
behavior-identical (see above).

---

## Issue #195 — Test seams: shared fakes package, interfaces for the derived caches, one FlowCache
Completed: 2026-09-18

### What was done

Three mechanical seams flagged by an `/improve-codebase-architecture` review, all in service of making
the four cache-backed ViewModels (`AllTimeStatsViewModel`, `TrainingLoadViewModel`,
`RepeatedRoutesViewModel`, `RepeatedIntervalsViewModel`) testable without standing up Hilt or a real
`ApplicationScope`.

**Shared fakes package** — moved every repository/location fake duplicated verbatim across
`HomeViewModelTest`, `SessionDetailViewModelTest`, and `MapViewViewModelTest` (`FakeIntervalRepository`,
`FakeRepeatedIntervalRepository`, `FakeDropboxSyncCursorRepository`, `FakeMapGraphRepository`, plus the
pre-existing `FakeCyclingSessionRepository`, `FakeLocationSource`, `FakeBestEffortRepository`) into one
new `app/src/test/.../fakes/` package, one fake per interface, and updated every importer (8 test files
beyond the three ViewModel tests: `RepeatedRouteRepositoryImplTest`, `RideRevealEvaluatorTest`,
`RideMilestoneEvaluatorTest`, `SessionComparatorTest`, `RideClassificationServiceTest`,
`PowerCurveAchievementEvaluatorTest`). `SessionDetailViewModelTest`'s local `FakeBestEffortRepository`
stub was dropped in favor of the shared one (behaviorally identical at its default empty-`records`
state). Test-only doubles specific to one test file (e.g. `FailingDeleteCyclingSessionRepository`,
`LatchedDeleteSessionsRepository`) stayed local — only the ones duplicated across files moved.

**Cache interfaces** — `AllTimeStatsCache`, `TrainingLoadCache`, `GlobalAverageCache`,
`RepeatedRoutesCache`, `RepeatedIntervalsCache` are now interfaces in `data/cache`, each implemented by
a `*Impl` class (still the `@Singleton @Inject constructor` doing the real Flow wiring), bound via a new
`di/CacheModule.kt` `@Binds` module — the same interface/impl/`@Binds` pattern `RepositoryModule`
already uses for repositories. Every ViewModel and `VelometricsApplication`'s eager-init injection site
kept referencing the cache by its original name, since that name now resolves to the interface — no
call-site changes needed outside the cache files themselves and the new module.

**One `FlowCache`** — `RepeatedRoutesCache`/`RepeatedIntervalsCache` were byte-identical modulo type, so
their hot-`StateFlow`-plus-`isLoading` shape moved into a new generic `HotFlowCache<T>`
(`data/cache/HotFlowCache.kt`); `RepeatedRoutesCacheImpl`/`RepeatedIntervalsCacheImpl` are now thin
wrappers around one each. `debounce(300)`'s value and rationale, previously copy-pasted across
`AllTimeStatsCache`/`TrainingLoadCache`/`GlobalAverageCache`'s KDocs, now live once in a new
`data/cache/CacheDebounce.kt` (`CACHE_DEBOUNCE_MS` constant + a `Flow<T>.debounced()` extension); all
three plus `HotFlowCache` call the shared extension instead of a bare `.debounce(300)`. Explicit decision
on the issue's open question: `HotFlowCache` **does** debounce (previously neither Repeated* cache did,
with no stated reason) — clustering does `deleteAll()` then one `saveX()` per cluster on every run
(see `RouteClusteringService`/`IntervalClusteringService`), the same per-write-storm shape the other
three caches' debounce already guards against, just triggered by a clustering pass instead of a bulk
import.

**Smoke tests** — one test class each for the four ViewModels that had none before, exercising the new
interface seam directly: `AllTimeStatsViewModelTest`, `TrainingLoadViewModelTest` (both trivial —
`uiState` is a direct passthrough of the injected cache's `StateFlow`, verified against a
`FakeAllTimeStatsCache`/`FakeTrainingLoadCache`), `RepeatedRoutesViewModelTest`,
`RepeatedIntervalsViewModelTest` (cover the cache passthrough plus sort/`isLoading` wiring and that
`refresh()` invokes the injected clustering service and clears `isRefreshing`, using
`FakeRepeatedRoutesCache`/`FakeRepeatedIntervalsCache` and a relaxed/`coEvery`-stubbed
`RouteClusteringService`/`IntervalClusteringService` mock — no Hilt, no `UserSettingsRepository`'s real
Android `DataStore`, no `ApplicationScope`).

### Tests
New: `AllTimeStatsViewModelTest` (2), `TrainingLoadViewModelTest` (2), `RepeatedRoutesViewModelTest` (3),
`RepeatedIntervalsViewModelTest` (3) — 10 new cases, all exercising the fakeable-interface seam this
issue introduced. `compileDebugKotlin`, `compileDebugUnitTestKotlin`, `assembleDebug` (confirms the new
`CacheModule` `@Binds` graph resolves), full `testDebugUnitTest`: all pass, 379 tests, zero
failures/errors across the whole suite (aggregated test-result XML). No on-device verification this
session (no emulator/device attached) — this issue is a pure test-seam refactor with no UI or behavior
change, so the unit suite is the relevant feedback loop.

---
