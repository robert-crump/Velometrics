## Issue #214 -- Session Narrative assembler + all-time Tag recap + Zone 2 content (supersedes #202)
Completed: 2026-09-19

### What was done

- New `SessionNarrativeAssembler` (domain/service): `build(session)` is the single path for tag-scoped comparison +
  interval fetch (once) + narrative generation; `observe(sessionId)` re-assembles when the session row's tag changes
  (backfill by `reclassifyAll`). Returns null for untagged rides and on repository errors. `SessionDetailViewModel`
  exposes `narrative: StateFlow<SessionNarrative?>` (headline + text), replacing `tagNarrative`.
- `SessionComparator`: `computeComparison(session)` no longer takes a tag; new `computeTagComparison(session, tag)` returns an
  all-time `TagComparison(sampleCount, PoolMedians)`. `PoolMedians` gains `fatGrams`. The tag-scoped last-5 query was removed
  from DAO/repository/fake (untagged Last5/AllPrevious drawer comparison is unchanged).
- `TagComparisonNarrative` now takes a `TagComparison`; Zone 2 renders the fixed 5-metric sentence (each metric drops out
  independently; first rendered one carries "in a typical Zone 2 ride"). Intervals/Recovery keep the single-sentence model,
  now on all-time medians (follow-on issues).
- UI: removed `RideTagLabel` chip/expand state; `RideSummaryGrid` shows always-visible "Vs. other [TAG] rides" + text, nothing
  when untagged.
- Tests: new `SessionNarrativeAssemblerTest`, Zone 2 cases in `TagComparisonNarrativeTest`, tag pool tests in
  `SessionComparatorTest`, recap assertions in `SessionDetailViewModelTest`.

`testDebugUnitTest` and `assembleDebug` pass. No visual check on a device this run.

## Issue #213 -- isCustomName flag on RepeatedRoute
Completed: 2026-09-19

### What was done

- `RepeatedRouteEntity`/`RepeatedRoute` gain non-null `isCustomName`; DB v18 -> v19 via `MIGRATION_18_19`
  (`ADD COLUMN isCustomName INTEGER NOT NULL DEFAULT 1`, so pre-existing rows are backfilled to true). 19.json schema exported.
- `RouteClusteringService`: brand-new clusters get `false`; a cluster matched to an existing route keeps that route's flag
  (it re-saves the existing name, so the flag must follow it).
- `renameRoute` in the repository sets `isCustomName = true`. No visible UI effect (prerequisite for #217).
- Tests: migration test (backfill true), repository rename test, new `RouteClusteringServiceTest` (new = false, matched keeps true).

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #212 -- Floating header controls over the map (Ride Detail Screen)
Completed: 2026-09-19

### What was done

- Removed the `TopAppBar` from `SessionDetailScreen`; the map Box now only pads for the Scaffold's bottom inset,
  so the map extends up under the status bar.
- Added floating 44dp circular back (top-left) and overflow (top-right, still holds "Delete ride" + confirm flow)
  buttons via a private `FloatingCircleButton` (solid `colorScheme.surface` background), inset with `statusBarsPadding()`
  and 12dp/8dp edge padding. They render in loading/not-found states too so back always works.
- Drawer date text (`RideSummaryGrid`) untouched. No visual check on a device this run.

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #210 -- HomeViewModel rewired onto WorkManager-backed Dropbox sync
Completed: 2026-09-19

### What was done

- `syncDropbox()`/`autoSyncDropbox()` now `enqueueUniqueWork(DROPBOX_SYNC_WORK_NAME, KEEP)` a `DropboxSyncWorker`
  (network CONNECTED; pull-to-refresh also expedited with RUN_AS_NON_EXPEDITED fallback; `isUserInitiated` via input data).
  `DROPBOX_SYNC_WORK_NAME` lives in `DropboxSyncWorker`; `WorkModule` provides `WorkManager` to Hilt.
- `isSyncing` derives from `getWorkInfosForUniqueWorkFlow`; `dropboxSyncMessage` and the `RideReveal` branch of
  `importState` derive from `DropboxSyncOutcomeStore.outcome` and are `consume()`d on clear. Manual import unchanged.
  `HomeViewModel` no longer takes `DropboxSyncService`.
- Tests: `HomeViewModelTest` reworked (enqueue params, isSyncing, outcome consume);
  new `HomeViewModelSyncWorkTest` (real test WorkManager: second enqueue is kept out by KEEP).
- NOT done: emulator-scripted `adb shell am kill` check (no device/emulator attached this run). NeedsReauth persistence
  is covered at the worker level (`markNeedsReauth`), no new test.

`testDebugUnitTest` and `assembleDebug` pass.

## Issue #209 — DropboxSyncWorker runs the Dropbox sync via WorkManager
Completed: 2026-09-19

### What was done

- New `data/dropbox/DropboxSyncWorker.kt` (`@HiltWorker` `CoroutineWorker`): same call sequence as
  `HomeViewModel.syncDropbox()` (connected/reauth checks, `captureBaseline`, `DropboxSyncService.sync()` reused as-is,
  `evaluate`), publishing `Reveal`/`Message` to `DropboxSyncOutcomeStore`. `isUserInitiated` is read from input `Data`
  (`KEY_IS_USER_INITIATED`, default true).
- `Completed`/`TransientFailure` -> `Result.success()` (no `retry()`); `NeedsReauth` -> `markNeedsReauth()` + `Result.failure()`.
- Recluster (route + interval, each failure logged and isolated) runs in `finally`, so on every path.
- `buildSyncMessage` moved out of `HomeViewModel` to internal `buildDropboxSyncMessage` so both share it.
  `HomeViewModel` is otherwise unchanged; wiring it to enqueue the worker is #210.
- `DropboxSyncWorkerTest` (Robolectric, `TestListenableWorkerBuilder`, mocked services, real outcome store).

`testDebugUnitTest` and `assembleDebug` pass.
