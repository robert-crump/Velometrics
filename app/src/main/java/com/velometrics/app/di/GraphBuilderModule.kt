package com.velometrics.app.di

// All services in this package (RTreeSpatialIndex, RoutePlanner, GpxExporter,
// GraphJsonImporter, PoiJsonImporter) are annotated with
// @Singleton and @Inject constructor() so Hilt provides them automatically.
// This module is intentionally empty.
